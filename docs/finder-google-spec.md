# Finder — Google Places, Google map and the Google-listing check (implementation contract v2, 12 Sep 2026)

Scope: Google as an additional source for `/admin/find` (place suggestions in the Area box, a Google map, Google places matched into the results), and a real Google-listing check in the audit and score. Repo `/home/hermes/workspace/digitalm`, branch `feat/finder-google` off `main` (sub-branches `feat/finder-google-backend`, `feat/finder-google-frontend`; every commit `finder-google: …`; never push; never commit `.env*`). This document wins over `docs/finder-ux-spec.md` and `docs/leadgen-build-spec.md` §6–§7 where they differ; everything not mentioned here is unchanged, and **with `GOOGLE_PLACES=off` (today's value) nothing changes at all** — the Leaflet/OpenStreetMap finder, the plain Area box, the hidden Google block and the unmeasured `google_listing` check stay exactly as they are.

v2 applies the first review round (terms-and-cost and engineering-fit lenses); the changes are listed in Appendix C. Where v2 and v1 differ, v2 wins.

Radu's request: *"why don't we just use Google Maps, also with place suggestions in the search? also check if they have Google My Business"*. Decision: yes — behind `GOOGLE_PLACES=on`, within the free monthly allowances, and inside Google's terms **as they apply to us**.

---

## 0. Read this first — the terms that govern, and where this contract departs from the brief

Digital M bills Google Cloud from France, so the **EEA Service Specific Terms** apply (last modified 10 Jun 2026, section B.15 "Places API (Legacy and New)"), not the non-EEA terms the brief was written against. Verified on 12 Sep 2026 (sources in the research memo behind this document). What they change:

| Brief said | The EEA terms say | This contract does |
|---|---|---|
| Places results may only be shown on a Google map, so switch the map to Google Maps | B.15.1 *No Use With any Map*: other than latitude, longitude and place_id, Places content may not be displayed on, next to or visually associated with **any** map, Google's included. B.15.3: this does not apply to the **Places UI Kit**. | A Google map is offered because Radu wants one (§5.2), not because it buys rights. Every Google name, address, rating, hour, website or photo shown near the map is rendered by a Places UI Kit element (Google renders it, with its own attribution). Our own code shows only pins (lat/lng + place_id) and our derived words — and the derived words never sit on a page while a map is visible on it (§3.4). |
| Store every Google field with `fetched_at`, refresh/drop after 30 days | B.15.4 + A.3: `place_id` may be kept indefinitely; latitude/longitude for **30 consecutive days**; nothing else. Policy: "must not pre-fetch, cache, or store Places API content beyond the allowed exceptions". ToS 3.3.2(a)(iii): no copying and saving of business names, addresses or reviews. ToS 3.2.4: attributions returned with the data must be displayed whenever the data is. | Only `place_id`, lat/lng (inside the 24 h search cache), **derived** booleans/counts from the audit (§3, decision D2) and the **attribution provider names** that came with them (an attribution notice, not place content — ToS 3.2.4) are ever written. No Google response is cached. Names/addresses from Google are used transiently for matching and then dropped. |
| Polygon-filter Google results | ToS 3.3.2(c)(iv): no point-in-polygon analysis on Places lat/lng. | Tiles are clipped by **our** polygon before the request; results are filtered by `addressComponents` (department / postcode / locality / country) — §4.3. A distance from one point (the circle areas, the match rule) is not a polygon analysis. |
| Merge Google rows (source `google`) into the list with their names | 3.3.2(a)(iii) again, plus 15.1 (list beside the map). | Google places that match an OpenStreetMap or register row enrich that row (`googlePlaceId`, *Also on Google*). Places found only on Google become **pins without a name**; opening one shows Google's own full listing panel (UI Kit `gmp-place-details`, which shows the website) and the way to save it is *Add by its website* — the URL typed by Radu, the name taken from the site (§5.5). |
| Autocomplete (New) proxied from the server with session tokens | Permitted use (1) covers autocompletion; predictions listed above the map are still content *next to a map* unless they come from the UI Kit autocomplete element (exempt). | The Area box uses the UI Kit `gmp-basic-place-autocomplete` element when Google is on (§5.3); its `gmp-select` event carries a place id only, which one Details Essentials call turns into a query for Nominatim. The proxied design is kept in Appendix B as the fallback if the element proves unusable. |
| "Check if they have Google My Business" | The Place resource has no claimed/verified/owner field; the Business Profile API forbids lead-generation use. | The check says *listing found* and whether it *looks maintained* (business status, website on the listing, hours, review count, photo count). The words "claimed", "verified" and "Google My Business" never appear (§4.6). |

Decisions this contract needs from Radu (defaults apply until he answers; each is one env value or one line to flip, nothing else moves):

- **D1 — Google as a discovery source.** The EEA "Permitted Uses" list covers autocompletion (1) and *visualising and managing Places content related to a sales team's customers or opportunities* (3). A saved prospect is an opportunity; open-ended "every restaurant in Ariège" discovery is not obviously any of the nine uses. Two further sentences bear on it: discovery pulls up to 30 × 20 places' names and addresses per search to match them, which reads close to ToS 3.3.2(a)(ii) *"bulk download Google Maps Content"* (the names are dropped after matching, but they are fetched in bulk); and folding Google's result set into a list built from other sources is at least in tension with ToS 3.3.2(d) *"modify … search results"*. Default: **`GOOGLE_PLACES_DISCOVERY=off`** — map, suggestions and the listing check work; searches do not call Google. Radu turns it on only if he is comfortable with all three readings.
- **D2 — Derived audit signals, and where they go.** Storing `{ operational, websiteOnListing, hours, reviews: 12, photos: 3 }` in the audit record is our measurement, not Google's content, but Google does not say so anywhere. Default: **stored**, never any string from Google (attribution provider names excepted, §3.5). The derived result also **leaves the CRM**: the `/r` report and the outreach drafts tell the prospect the state of their listing. EEA permitted use (3) covers *internal* visualisation of a sales team's opportunities; using Places-derived findings in outbound material to the business itself is a further reading Radu has to make. Conservative default applied here: outside the CRM the finding is **status words only** — *Votre fiche Google est en place et complète* / *… existe, mais elle est incomplète* / the existing *not found* sentence (only after Radu marked it so, §4.5) — never a per-signal enumeration (no "no hours", no review or photo counts) in the report, the draft prompt or the templates.
- **D3 — The listing's website.** Copying `websiteUri` into `prospects.website` would give many no-website prospects an auditable site — and is copying content. Default: **not copied**; the full UI Kit panel (§5.7) shows the website live and Radu types it into the website field himself; the business name is not read off the panel either — Add by URL takes it from the site (§5.5).
- **D4 — Google map or Leaflet.** The Google map costs one Dynamic Maps load per finder page open (10,000 free/month) and grants no display rights. Default: **Google map when `NEXT_PUBLIC_GOOGLE_MAPS_KEY` and `NEXT_PUBLIC_GOOGLE_MAP_ID` are set**, Leaflet otherwise and on any load failure.
- **D5 — Autocomplete.** Default: UI Kit element (§5.3). Fallback: Appendix B.

Free-tier discipline, in one table (every call names its SKU in code — `google.ts` refuses a request whose field mask crosses the tier the call was budgeted for; §4.2):

| Call | Where | SKU | Free/month | Our cap (`api_usage` provider) |
|---|---|---|---|---|
| Text Search (bbox tile + trade; circle + trade text) | discovery (D1), save-time match, manual-match candidates | Text Search **Pro** | 5,000 | `google_search` ≤ `GOOGLE_SEARCH_MONTHLY_CAP` (4,500); discovery stops 500 earlier (§4.3) |
| Nearby Search (circle areas with a Table A type) | discovery (D1) | Nearby Search **Pro** | 5,000 | `google_search` (same pool) |
| Place Details, Enterprise mask | listing check of a saved prospect | Place Details **Enterprise** | 1,000 | `google_details_enterprise` ≤ `GOOGLE_PLACES_MONTHLY_CAP` (900) |
| Place Details, Essentials mask | closing an area suggestion (location + address components) — one per pick | Place Details **Essentials** | 10,000 | `google_details_other` ≤ `GOOGLE_DETAILS_MONTHLY_CAP` (4,500) |
| Maps JavaScript map | finder page (browser) | Dynamic Maps | 10,000 | not countable server-side → **per-day quota cap** in Cloud Console (§6) + budget alert |
| UI Kit place details (full and compact) / autocomplete | card, prospect page, Area box (browser) | Places UI Kit Query / Autocomplete Per Session | 10,000 each | idem |
| Place Photos media | — | — | — | **never called**: the UI Kit renders photos; the audit counts `photos[].length` (free in Details) |

There is no Place Details Pro call: manual-match candidates come from the one Text Search Pro request that already carries `displayName`/`formattedAddress` (§4.5). Browser-side SKUs have no server-side counter and a budget alert only emails after spend, so the enforceable hard stops for the browser key are the **per-day request quotas** set in Cloud Console (§6, L1); the €5 budget alert is the secondary signal. Server-side pools are reserved before the socket (§4.2) so a timeout or two concurrent callers cannot overshoot the caps; the 10 % headroom below each free allowance exists for the billing-window skew described in §6.

---

## 1. What Radu sees (Google on)

1. He types **"arie"** in the Area box: Google's suggestion list opens under it (*Ariège, France* · *Ariège, river*…), keyboard-navigable. He picks *Ariège, France*; the element clears the box (that is how it behaves), the finder resolves the pick through Google (one Essentials call) then Nominatim as today — no chooser, because the pick's coordinates settle the ambiguity — the box shows *Ariège, France* again once the search starts, and the status line reads *Ariège — department, France · about 292 restaurants on the map*.
2. The map is **Google Maps**: the dashed orange Ariège outline, the OpenStreetMap/register pins in the same colours as today, numbered discs where pins crowd, the progress bar on top. Hover, click, *List follows the map*, chips: identical.
3. With discovery on (D1): after the register phase the progress line says *Asking Google — 6 of 9 parts of Ariège*; **blue pins** appear for places Google knows that neither OpenStreetMap nor the register listed (*23 only on Google* in the list header, a chip to hide them). Rows that Google also knows get *Also on Google · View on Google Maps* in their card. Clicking a blue pin opens a card whose body is Google's own listing panel (name, address, website, phone, hours, rating, a photo, Google's attribution) with **Add by its website** and **Not this one**.
4. **Save & audit** on a matched row saves the place id with the prospect. The audit's *Google listing* check now scores: *Listing found · looks maintained* (10/10), *looks unmaintained* (partial), *No listing found* (0/10, flag *no Google listing found* — only after Radu marked it so). Prospects saved without a match are matched automatically during their audit (name + distance); when nothing matches, the page says *No match found automatically — match manually or mark not found* and the check stays unmeasured. The old *Find on Google* button becomes *Check again* / *Match manually* / *Not this listing*.
5. The prospect page's **Google listing** section (below the fold, collapsed) shows the status in words, when it was checked, the signals in words (*website on the listing · opening hours filled in · 37 reviews · 3 photos*) with the Google Maps logo and *Data: …* when Google named a data provider, **View on Google Maps**, and *Show the Google listing* → Google's full panel; while the section is open the mini map is hidden, and the signal words appear nowhere else on the page.
6. When the allowance is used up, or Google does not answer, one sentence says so (*The monthly Google allowance is used up — try again next month.*); nothing else breaks. With `GOOGLE_PLACES=off`, none of the above exists.

---

## 2. Ownership (two engineers in parallel, one integrator)

### 2.1 Backend — exclusive

- `src/lib/discover/**`: **new** `googleRequests.ts` (pure: field masks, request bodies, response parsers, `signalsOf`, `placeIdOk`), `googleTiles.ts` (pure), `googleInside.ts` (pure), `googleMatch.ts` (pure), `googleCheckRules.ts` (pure: the save-time match / listing-check decision table and the column writes as data, §4.5), `googleDiscover.ts` (the search phase), `googleSuggest.ts` (pick resolution), `googleCheck.ts` (I/O: runs `googleCheckRules` with `googleFetch` and `enquiriesDb`), rewritten `google.ts` (the I/O client: caps, reservations, counters, errors); tests `googleRequests.test.ts`, `googleTiles.test.ts`, `googleInside.test.ts`, `googleMatch.test.ts`, `googleCheckRules.test.ts`; fixtures `src/lib/discover/fixtures/google/*.json` (synthetic, §8.1); edits in `runner.ts`, `progress.ts`, `notes.ts` (+ tests), `dedupe.ts` (`googlePlaceId` propagation), `geocode.ts` (`resolveArea` hint), `adminChildren.ts` + `plan.ts` (children carry a bbox, §4.3), `index.ts` (exports).
- `src/lib/audit/{checks.ts, score.ts, job.ts}` (+ tests; `job.ts` also the site-name write-back of §4.5 and the Google-only daily allowance), `src/lib/report/findings.ts` (+ test), `src/content/auditChecks.ts` (the FR/EN copy of the check).
- `src/lib/prospects/store.ts` (+ `googleSignals`, `enqueueAudit` options, `addByUrl` place attach, `auditNext` clause), `src/lib/inbox/today.ts` (+ test), `src/lib/crm/http.ts` (one `HOSTS` line), `src/lib/crm/apiUsage.ts` (**unchanged** — `google.ts` passes its own day and month keys, §4.2).
- Routes: `src/app/api/admin/find/route.ts` (`suggestion`, `pins` query), `find/dismiss/route.ts` (`google:` keys), `prospects/[id]/google/route.ts` (rewritten), `prospects/route.ts` (`googlePlaceId` on Add by URL), `prospects/[id]/route.ts` (GET fields).
- Prospect-facing privacy texts and the GDPR records of §2.3 (backend only, one sentence each).
- `scripts/digitalm-purge.js` + its mirror in `/home/hermes/workspace/scripts/` (step 12), `scripts/digitalm-digest.js` + mirror (the two Today cards).

### 2.2 Frontend — exclusive

- `src/components/admin/**`: **new** `googleMaps.ts` (loader singleton + `googleMapsPlaceUrl` + `createPlacesElement`, tested in `format.test.ts`), `GoogleFindMap.tsx`, `GoogleAreaInput.tsx`, `GoogleListingPanel.tsx` (the UI Kit details element wrapper, full and compact), `GoogleOnlyCard.tsx`, `GoogleAttribution.tsx`; rewritten `GoogleBlock.tsx`; edits in `FindWorkspace.tsx`, `FindList.tsx` (chip, header count), `Legend.tsx`, `BusinessCard.tsx` (Google section), `AreaInput.tsx` (delegation), `AddByUrl.tsx` (`placeId` prefill, name optional), `AuditBlock.tsx` (status words only), `MiniMap.tsx` (`data-testid="mini-map"` + the hidden-while-Google-open class), `TodayCards.tsx`/`todaySummary.ts` + `todaySummary.test.ts` (the two housekeeping lines), `finderApi.ts` (shapes), `wording.ts` + `wording.test.ts` (`GOOGLE_TEXT`), `src/app/globals.css` (inside the existing `/* @@admin */` block: `.dm-gpin*`, `.dm-gcluster`, `.dm-google-attr`).
- `public/brand/google-maps-logo.svg` and `public/brand/google-maps-logo-outlined.svg` — the official "Google Maps" logo assets from Google's attribution page (the outlined variant is the one Google provides for use over map imagery), committed on **day 0**, before any component renders attribution.
- Pages `src/app/(tools)/admin/(gated)/{find/page.tsx, prospects/[id]/page.tsx}` (props and block order only — `GoogleBlock` moves below `AuditBlock`, §5.6).
- `next.config.mjs` — the `/admin/:path*` CSP entry of §5.1 and nothing else.
- `package.json` + lock — adds `@googlemaps/markerclusterer@2.6.2` and dev `@types/google.maps@^3.66`; nothing else.
- `scripts/e2e/finder.cjs` — items G-e2e of §9 appended.

### 2.3 Shared, append-only under markers (or one named sentence)

| File | Marker / place | Inserted by | Appended / edited by |
|---|---|---|---|
| `src/lib/crm/types.ts` (end of file) | `// @@finder-google:types` | backend, day 0 | backend only (§4.1); frontend imports through `finderApi.ts` |
| `src/lib/crm/schema.ts` `EXTRA_COLUMNS` | existing `// @@crm:finder` | — | backend (§4.1) |
| `.env.example` | existing `# @@crm:finder` | — | backend rewrites the three `GOOGLE_*` lines (`GOOGLE_PLACES`, `GOOGLE_PLACES_KEY`, `GOOGLE_PLACES_MONTHLY_CAP`) into the §6 block |
| `src/content/fr.ts` / `src/content/en.ts` — the prospecting privacy notice body (*Prospection commerciale auprès des professionnels* / *Business-to-business prospecting*) | the one `body` string | — | backend only, the §4.9 wording; nothing else in these files |
| `docs/leadgen-registre-art30.md` (rows *Sources*, *Destinataires*/*Recipients*, *Transferts*/*Transfers*), `docs/leadgen-lia.md` (§2 "not collected" bullet, §3 processors bullet, §5 review paragraph) | the named rows / bullets | — | backend only (§4.9) |
| `README.md` "Admin CRM" (after the finder-ux markers) | `<!-- @@finder-google:backend -->` then `<!-- @@finder-google:frontend -->` | backend, day 0 | each engineer under their own marker |
| `docs/leadgen-build-spec.md` (end of file) | `## 17. finder-google` | backend | backend only — a pointer to this document plus the §4.8 API additions |

### 2.4 Rules

- No other shared edits. `src/lib/crm/{apiCache,apiUsage,auth,db,time,classify,jobs,jobHandlers}.ts`, `src/instrumentation.ts`, `src/middleware.ts` untouched; no new job kind (the listing check runs inside the audit job). One `next build` at a time on the box — only the integrator builds staging; engineers run `NODE_OPTIONS=--max-old-space-size=1536 npx tsc --noEmit` and `npm test`. Never touch `digitalm-prod/`.
- Day 0 (backend, first two hours): §4.1 types + schema + `.env.example` block, `notes.ts` codes with text, the synthetic fixtures (§8.1) and `googleRequests.ts` masks committed on `feat/finder-google` — the frontend builds against `search-ariege.sample.json` extended with `googlePins` and `progress.google`. Day 0 (frontend, first hour): the two logo files under `public/brand/`.
- No keys exist yet: every Google call is a no-op with a note when `GOOGLE_PLACES!=on` or a key is missing; unit tests use fixtures; the live checklist (§8.3) runs once Radu's billing is on.
- The integrator resolves contract questions by appending to Appendix C.

---

## 3. Google's terms as code rules (each has a test or a live-checklist item)

1. **Persisted Google data = `place_id`, lat/lng (≤ 30 days), derived booleans/counts, attribution provider names.** `googleRequests.test.ts` asserts `signalsOf()` returns only booleans, numbers, `fetchedAt` and `attributions: string[]` (provider names, nothing else) — no other key, no other string; `googleDiscover` returns `GooglePin { placeId, lat, lng, fetchedAt }` and row patches `{ googlePlaceId, onGoogle }` only; a static test reads `google.ts`/`googleDiscover.ts`/`googleCheck.ts`/`googleCheckRules.ts` and asserts they import nothing from `apiCache` and never call `console.log/error` with a response body. Live: `SELECT COUNT(*) FROM api_cache WHERE provider LIKE 'google%' OR payload LIKE '%displayName%' OR payload LIKE '%formattedAddress%'` = 0 after a search and an audit.
2. **No Google response cached, no bulk pre-fetch.** The client never uses `cached()`; discovery runs only inside a search Radu started, never for the over-cap gate, never for `plan` children, never for units that did not run; *Run again* re-asks Google (counted); discovery stops when the search pool is within 500 of its cap so listing checks keep running.
3. **No point-in-polygon on Google coordinates.** `googleInside()` reads `addressComponents` only; `polygon.ts` functions are called on **our** tiles' corners, never on a place's `location` (`googleTiles.test.ts` + a grep-style test that `googleDiscover.ts` and `googleInside.ts` do not import `pointInPolygon`). A distance from a single point (circle areas, the 150 m / 300 m match rule) is allowed.
4. **No Google content beside a map except through the Places UI Kit — and our derived words never share a page with a visible map.** Google-only pins carry no name (`title` empty, tooltip *On Google only*); the Google-only card body and the prospect page's *Show the Google listing* panel are the full `<gmp-place-details>` element, the business card's collapsed panel is `<gmp-place-details-compact>`; the prospect page hides `MiniMap` while its Google section is open, and the derived booleans/counts (signal words, review and photo counts, *Data: …*, manual-match candidates) render **only inside that open section** — `AuditBlock`, the report and every other spot show status words only (§4.6, §5.6). e2e G8, G9.
5. **Attribution.** The `GoogleAttribution` component **always renders the Google Maps logo** (`public/brand/google-maps-logo.svg`, 16–19 px high, 10 px clear space; the outlined file when the mark sits over map imagery, i.e. the Legend panel), committed day 0 — no runtime or build-time existence check. The text form *Google Maps* (`translate="no"`, never localised) is used in exactly one place where space is genuinely limited: the list-header count *· 23 only on Google*. Where attribution applies: the *Also on Google* line, the signals block and the candidate list on the prospect page, the list-header count, the Legend line. **Status words** (*Listing found · looks maintained*, *No listing found*, the report's pass/partial/fail sentence, `AuditBlock`'s line) are our audit result and carry no attribution — the prospect-page status badge has none. `attributions[]` returned by Details or Text Search (third-party data-provider names) are **kept with the signals and the candidates** and rendered as *Data: {names}* wherever the signals or a candidate appear, from the stored value — ToS 3.2.4 holds for as long as the data is shown, not only for the request that produced it. UI Kit panels carry Google's own attribution.
6. **Field masks by tier.** `MASK_SEARCH_PRO`, `MASK_DETAILS_ENTERPRISE`, `MASK_DETAILS_ESSENTIALS` are frozen constants (three — there is no Pro Details mask); `googleRequests.test.ts` holds the tier table of Appendix A and fails if any mask contains a field of a higher tier than its name. `google.ts` refuses (throws `GoogleError("mask_tier")`) a request whose mask is not one of the three constants.
7. **Keys.** The server key is read in `google.ts` only, sent as a header, never logged, never in a URL; the browser key is `NEXT_PUBLIC_` and restricted (§7). A test asserts no file under `src/components` references `GOOGLE_PLACES_KEY`.
8. **Words.** "claimed", "verified", "Google My Business", "GMB" never appear: `wording.ts` `FORBIDDEN_TOKENS` adds `/\bclaimed\b/i`, `/\bverified\b/i`, `/Google My Business/i`, `/\bGMB\b/` (whole-word, so *unverified* in a status token is not caught — it is never shown as text anyway); `notes.ts` `FORBIDDEN_TOKENS` (substring match) adds `"claimed"`, `"Google My Business"`, `"GMB"` — **not** `"verified"`, which would match *unverified* — and `notes.test.ts` asserts `length === 13`. **No `GOOGLE_TEXT` sentence and no note text may contain *tile(s)* or *partial*** (both already forbidden by finder-ux §7): the vocabulary is *parts of {area}*; `tiles`/`tilesDone` survive only as code identifiers. "Google Business Profile" only in the prospect-facing check copy where it already is.

---

## 4. Backend

### 4.1 Types, schema, switches

Appended to `src/lib/crm/types.ts` under `// @@finder-google:types` (the `googleAdapter` stub above it stays; its `search()` is no longer called — the runner calls `googleDiscover` directly, and `enabled()` remains the single on/off truth):

```ts
// @@finder-google:types — docs/finder-google-spec.md §4.1. Backend appends here; frontend imports only.
export type GoogleMatch = "auto" | "manual";
/** Derived from one Place Details answer; never a string from Google except the attribution provider names (ToS 3.2.4). */
export interface GoogleSignals { operational: boolean | null; websiteOnListing: boolean; hours: boolean; reviews: number; photos: number; fetchedAt: string; attributions: string[] }
/** A place Google knows that no other source listed — lat/lng may live at most 30 days (the search cache keeps them 24 h). */
export interface GooglePin { placeId: string; lat: number; lng: number; fetchedAt: string; hidden?: boolean; saved?: { prospectId: number; reference: string } }
export interface GoogleProgress { state: UnitState; tiles: number; tilesDone: number; requests: number; found: number; matched: number; only: number; dropped: number; pinsVersion: number; error?: "busy" | "timeout" | "network" | "refused" | "allowance" }
export interface GoogleUsage { checks: { used: number; cap: number }; searches: { used: number; cap: number }; other: { used: number; cap: number } }
/** Transient (§4.5) — never stored; `attributions` are the provider names Google returned with the candidate. */
export type GoogleCandidate = { placeId: string; name: string; addressLine: string; distanceM: number | null; attributions: string[] };
export type GoogleListingReason = "no_match" | "no_location" | "allowance" | "unavailable";
```

Optional fields added in place (same file, same block rule — the backend edits its own earlier blocks): `Business.googlePlaceId?: string`; `ResultRow.onGoogle?: boolean`; `SearchProgress.google?: GoogleProgress`; `SearchResultV2.googlePins?: GooglePin[]` (present only when the Google phase ran, §4.3); `Prospect.googleCheckedAt?: string | null`, `Prospect.googleMatch?: GoogleMatch | null`, `Prospect.googleSignals?: GoogleSignals | null` — **optional**: filled only by `getProspect` / `GET /api/admin/prospects/[id]` through `store.googleSignals(id)` (read from the latest audit's `checks`, not a column); `rowToProspect` and the list rows leave them undefined so `listProspects` parses no audit JSON.

Schema — `EXTRA_COLUMNS` under `// @@crm:finder`: `["prospects","google_checked_at","TEXT"]` (last match/check attempt), `["prospects","google_match","TEXT"]` (`auto|manual|NULL`). Existing columns `google_place_id`, `google_listing`, `google_confirmed_at` keep their meaning (`confirmed_at` = when the state was set).

Switches (`google.ts`): `googlePlacesOn()` = `GOOGLE_PLACES === "on" && !!GOOGLE_PLACES_KEY` (unchanged); `googleDiscoveryOn()` = `googlePlacesOn() && GOOGLE_PLACES_DISCOVERY === "on"`; `googleMapConfigured()` = `googlePlacesOn() && !!NEXT_PUBLIC_GOOGLE_MAPS_KEY && !!NEXT_PUBLIC_GOOGLE_MAP_ID` (server-side read of the public vars, passed to pages as props). `GOOGLE_PLACES=on` without a key → note #17 on the find page and the Today card `google-key` (§4.7).

### 4.2 The client (`google.ts`, I/O) and the pure request module (`googleRequests.ts`)

`googleRequests.ts` (pure, tested, no imports beyond types and `classify.ts`):

- Masks (Appendix A tiers): `MASK_SEARCH_PRO = "places.id,places.displayName,places.formattedAddress,places.addressComponents,places.location,places.types,places.primaryType,places.businessStatus,places.pureServiceAreaBusiness,places.attributions,nextPageToken"` (note: no `places.photos` — Pro in search, unneeded); `MASK_DETAILS_ENTERPRISE = "id,businessStatus,websiteUri,nationalPhoneNumber,regularOpeningHours,rating,userRatingCount,photos,pureServiceAreaBusiness,attributions"`; `MASK_DETAILS_ESSENTIALS = "id,formattedAddress,addressComponents,location,viewport,types"`. There is no `MASK_DETAILS_PRO`.
- Builders: `textSearchBody({ textQuery, includedType, rect?, bias?, languageCode, regionCode, pageToken? })` → `{ textQuery, includedType?, strictTypeFiltering: !!includedType, languageCode, regionCode, pageSize: 20, locationRestriction: { rectangle: { low: {latitude: s, longitude: w}, high: {latitude: n, longitude: e} } } | locationBias: { circle: { center, radius } }, pageToken? }` (`rect` and `bias` are exclusive; `bias.radius` clamped to 50,000); `nearbySearchBody({ includedTypes, center, radiusM })` (radius clamped to 50,000); `matchSearchBody({ name, city, lat?, lng?, countryCode })` → `textQuery: "<name> <city>"`, `locationBias: { circle: { center, radius: 2000 } }` when coordinates exist else `regionCode` only, `pageSize: 5`. Trade table `GOOGLE_TYPES: Record<categoryKey, { includedType: string | null; query: { fr: string; en: string } }>`: restaurant→`restaurant`, bar→`bar`, hotel→`hotel`, gite→`bed_and_breakfast`, campsite→`campground`, bakery→`bakery`, butcher→`butcher_shop`, hairdresser→`hair_salon`, beauty→`beauty_salon`, garage→`car_repair`, plumber→`plumber`, electrician→`electrician`, joiner→**null** (text "menuisier" / "joiner"), painter→`painter`, roofer→`roofing_contractor`, estate_agent→`real_estate_agency`, optician→**null** (text "opticien" / "optician"), dentist→`dentist`, gym→`gym`; custom trades → null + the custom label. `textQuery` language follows the area's country (`fr` for FR/BE/CH/LU/MC/AD, else `en`); `regionCode` = area country code lower-case.
- Parsers (return **our** shapes, drop everything else): `parseSearch(json) → { places: GooglePlace[]; nextPageToken: string | null }` with `GooglePlace = { placeId, name, addressLine, lat, lng, postcode: string | null, country: string | null, adminLevel1: string | null, adminLevel2: string | null, locality: string | null, postalTown: string | null, operational: boolean | null, serviceArea: boolean, attributions: string[] }` (`name` and `addressLine` exist only for the matching step and the transient candidate list; they never reach a store or a log); `parseDetailsEnterprise(json) → GoogleSignals` via `signalsOf`: `operational = businessStatus === "OPERATIONAL" ? true : businessStatus ? false : null`, `websiteOnListing = !!websiteUri`, `hours = !!regularOpeningHours?.periods?.length`, `reviews = userRatingCount ?? 0`, `photos = photos?.length ?? 0`, `fetchedAt = now`, `attributions = (attributions ?? []).map(a => a.provider).filter(Boolean).slice(0, 5)` (≤ 80 chars each); `parseDetailsEssentials(json) → { placeId, lat, lng, countryCode, adminLevel1, adminLevel2, locality, postalCode, kindHint: AreaKind | null, query: string }` where `kindHint` comes from `types` (`country`→country, `administrative_area_level_1`→region, `administrative_area_level_2`→department, `locality|postal_town|sublocality`→town, `postal_code`→postcode, else place) and `query` = the component matching the primary type + ", " + country long text (fallback `formattedAddress`); `candidatesOf(places, prospect) → GoogleCandidate[]` (name, first line of `formattedAddress`, distance when both have coordinates, attributions); `parseError(status, json) → GoogleErrorCode`. `placeIdOk(id)` = `/^[A-Za-z0-9_-]{10,72}$/` (real ids are ~27 chars; 72 keeps `google:<id>` inside the dismiss route's 80-char key cap).

`google.ts` (I/O; thin, covered by the live checklist): `googleFetch<T>(kind: "search" | "details_enterprise" | "details_essentials", path, { method, body?, mask, signal, retry?: boolean, reserve?: number })`:

1. Off → throws `GoogleError("off")`; key missing → `GoogleError("no_key")`; mask not one of the three constants → `GoogleError("mask_tier")`.
2. Cap for the kind's pool (`google_search` / `google_details_enterprise` / `google_details_other`) reached — `used + 1 > cap − (reserve ?? 0)` — → `GoogleError("allowance", pool)` **before** any socket.
3. **Reserve the count before the socket**: `countApiUsage(pool, 1, googleDay())`. Then `spaced("google", 100)` → `fetchJson(HOSTS.googlePlaces + path, { method, headers: { "content-type": "application/json", "x-goog-api-key": key, "x-goog-fieldmask": mask }, body, timeoutMs: 15_000, signal })` — `HOSTS.googlePlaces = "https://places.googleapis.com"` is the one line added to `crm/http.ts`.
4. Success → parse. `HttpError` → `GoogleError`: 403 → `refused` (key restriction / API not enabled; logged as `google: refused (403)` and nothing else), 400 → `bad_request`, 429 → `busy` (Retry-After honoured **once, ≤ 30 s, only when the caller passed `retry: true`** — the runner phase does, routes never do, so a route answers within its budget), timeout/network → `timeout`/`network`, other 5xx → `unavailable`. On `refused` and `bad_request` (Google rejected the request without processing it) the reservation is released with `countApiUsage(pool, -1, googleDay())`; a timeout, `busy` or 5xx stays counted because Google may have processed it. Response bodies and the key are never logged.

Month key: Google's free tier resets on the 1st at midnight **Pacific**, so the three `google_*` pools are counted on the Pacific civil day — `googleDay() = zonedDateString("America/Los_Angeles")` (`time.ts`, already there) is passed as the `day` of `countApiUsage` and `googleMonthKey() = googleDay().slice(0, 7)` as the `month` of `apiUsageMonth`; `apiUsage.ts` itself is unchanged. `googleUsage(): GoogleUsage` reads the three pools for `googleMonthKey()` against `GOOGLE_PLACES_MONTHLY_CAP` (900), `GOOGLE_SEARCH_MONTHLY_CAP` (4,500), `GOOGLE_DETAILS_MONTHLY_CAP` (4,500) through `intEnv`. The legacy `google_places` grand total is no longer written; Today reads the pools (§4.7).

### 4.3 Discovery phase (`googleDiscover.ts`, runner integration) — only with D1 on

Runner (`runner.ts`): a new phase after the register phase and before `merge`, when `sources.includes("google")`:
- `!googlePlacesOn()` → note #10 `google_off` (as today); on but `!googleDiscoveryOn()` → note #18 `google_discovery_off`; both cases: no request, `progress.google` absent, `result.googlePins` **absent** (never an empty array; the key exists only when the phase ran).
- Otherwise `emit({ type: "stage", stage: "google" })` (`SearchProgress.stage` gains `"google"`; the UI says *Asking Google — 6 of 9 parts of Ariège*), then `googleDiscover({ area, category, expected: plan.expected, osmRows, registerRows, ran, signal, budget: GOOGLE_SEARCH_MAX_REQUESTS })` returns `{ patches: Map<key, { googlePlaceId }>; pins: GooglePin[]; progress: GoogleProgress; notes: SearchNote[] }`. Patches are applied to the identity rows before `mergeBusinesses`; `dedupe.ts mergeCluster` copies `googlePlaceId` from any member (first by `IDENTITY_RANK`) and sets `onGoogle` in `mergedToRow`. Pins go to `result.googlePins` (hidden keys `google:<placeId>` from `searches.dismissed` mark `hidden: true` on read, like rows); `progress.google.pinsVersion` is incremented whenever the pin list changes (once at the end of the phase in practice). A failed phase (`busy` three times, `refused`, `allowance`) ends with `progress.google.state = "failed"` + note #19/#20/#21; the search status is **not** `partial` because of Google alone (the OpenStreetMap/register result stands, `resolveStatus` ignores the Google phase). **`continue` is unchanged** (it still refuses a `done` search): a failed Google phase is re-asked through *Run again*, which reuses the 24 h OpenStreetMap/register cache and so re-requests only Google — the muted line under the finished status says so (§5.4).
- Pool reserve: the phase stops (note #21) when `googleUsage().searches.used ≥ GOOGLE_SEARCH_MONTHLY_CAP − GOOGLE_SEARCH_RESERVE` (`GOOGLE_SEARCH_RESERVE = 500`, a constant in `google.ts`, passed as `reserve` to `googleFetch`), so the save-time match and manual matching keep their requests for the rest of the month.
- Re-run and capped searches: Google is asked again (no cache). A `capped` search asks Google only for the tiles intersecting the units that ran: `runner` passes `ran: Bbox[]` = `unit.bbox` for tile-mode units, `unit.bbox` for children-mode units too — `PlanUnit.bbox` is now filled for children by `fetchChildren`, which adds `bb` to its Overpass statement (`out tags center bb;`, `ChildRel.bbox`, cache key bumped to `(selector, L, "v2")`) — and, when a unit has no bbox (an older cached plan), the whole area with note #25 `google_all_parts`.

Tiles (`googleTiles.ts`, pure, tested): `initialTiles(area, expected, ran?: Bbox[])`: `n = clamp(ceil(((expected ?? 60) × 1.3) / 50), 1, 16)` cells on a `ceil(√n) × ceil(√n)` grid over `area.bbox`; drop cells whose rectangle does not intersect the display polygon (bbox overlap **and** any corner or the centre inside via `pointInPolygon` on our tile points — rule 3); when `ran` is given, keep only cells intersecting at least one of those boxes; order from the centre outward. `kind = place` areas (circles): with a Table A type, one Nearby Search (`radiusM = area.radiusKm × 1000`, ≤ 50 km); with a null-type trade (joiner, optician, custom labels — Nearby Search takes types only, no text), one **Text Search** with the trade text and `locationBias.circle` (same radius, same `google_search` pool, same Pro SKU), whose results farther than `radiusM` from the centre are dropped (a distance, counted in `dropped`). Paging: up to 3 pages per tile; a tile whose third page is full (60 results, no `nextPageToken`… or a token still present) is **saturated** → `splitTile` into four quadrants (depth ≤ 3) appended to the queue. Budget: `GOOGLE_SEARCH_MAX_REQUESTS` (default 30) requests per search; when it runs out the remaining tiles are `skipped` and note #22 `google_incomplete` says how many parts were covered. Repeats across overlapping pages are de-duplicated by `placeId`.

Inside filter (`googleInside.ts`, pure, tested) — `googleInside(place, area): "yes" | "approx" | "no"`, from address components only: `country ≠ area.countryCode` → no. Then by kind: **department** (FR): `postcode` prefix = department code (`09`, `2A/2B → 20`, `97x`) or `adminLevel2 ≈ area.label` → yes; a present but different postcode/department → no; nothing present → approx. **region** (FR): `adminLevel1 ≈ area.label` → yes / different → no / absent → approx. **town / postcode**: `postcode ∈ admin.postcodes` or `locality|postalTown ≈ area.label` (or any commune name of a multi-commune postcode) → yes / different → no / absent → approx. **country**: yes. **place** (circle): yes (the Nearby circle or the distance check is the area). Non-FR department/region: `adminLevel1|2 ≈ area.label` (normalised, accent-insensitive) → yes/no/approx. `≈` = `normaliseName` equality. `no` → dropped and counted (note #23 `google_outside_dropped`); `approx` → kept; Google pins carry `inside` implicitly (the card says *Outside {area}?* only for rows, never for anonymous pins).

Matching (`googleMatch.ts`, pure, tested) — `matchPlace(place, rows: MatchRow[]): { key: string } | null` where `MatchRow = { key, name, lat?, lng?, geoSource, postcode? }`: candidates share a name key (`normaliseName`) **or** a trade-word-stripped token overlap ≥ 0.6 (Jaccard over tokens ≥ 3 chars; the stripped words are the category's FR/EN labels and `restaurant|bar|hotel|hôtel|salon|garage|cabinet|sarl|sas|eurl`); a candidate with source coordinates must be within **150 m**; a candidate without (register rows placed at the centre) must share the postcode; the nearest wins. `googleDiscover` matches each place against `osmRows` then `registerRows`; matched → patch; unmatched → pin. `matched`/`only`/`dropped` counts feed note #24 `google_matched` (folded into the finished line: *… 372 from the register (54 in both) · 201 also on Google · 23 only on Google*).

Reading a search (`getSearch`): Google pins get their `saved` field from one query — `SELECT id, reference, google_place_id FROM prospects WHERE google_place_id IN (…) AND deleted_at IS NULL` over the pin ids (chunks of 500) — the Google counterpart of `markAlreadySaved(rows)`, so a pin saved through *Add by its website* reads as saved on the next poll. `dismissSearchRow` accepts a key present in `cached.rows` **or** in `cached.googlePins` (`google:<placeId>`, validated with `placeIdOk`); anything else stays `404`.

### 4.4 Area suggestions — resolving a pick (`googleSuggest.ts`)

The UI Kit element's `gmp-select` event returns a `Place` containing **only the place id** and the element clears the input on selection (Appendix A). So `POST /api/admin/find` accepts `suggestion?: { placeId: string }` — nothing else — and `area` may be the last text Radu typed **or empty**. Server: `placeIdOk` else `400 bad_suggestion`; when Google is on, `resolveSuggestion(suggestion)` = Details **Essentials** (`MASK_DETAILS_ESSENTIALS`, `google_details_other`, ≤ 4 s, never `retry`) → `{ query, lat, lng, countryCode, kindHint }` — exactly one request per pick, no client-coordinates branch; then `resolveArea(query, undefined, hint)` where the new optional `hint: { lat, lng, countryCode, kind? }` makes `geocode.ts` **auto-pick** among ambiguous candidates: keep those whose country matches, then the one whose bbox contains the point (and whose kind matches the hint when given); no candidate fits → the `409 ambiguous` chooser as today. `searches.query_area` stores the derived query (*Ariège, France*) and the `202` response returns it as `queryArea` so the Area box can show it. Any Google failure (off, allowance, refused, busy, timeout): with a non-empty `area`, fall back to `resolveArea(area)` with the typed text (the search never fails because of a suggestion when there is text to fall back on); with an empty `area`, answer `400 suggestion_unresolved` with `message` *Google could not resolve that place — type the area instead* (never `resolveArea("")`). Rate: `rateLimit("google-suggest:" + ip, 60, 10 min)` on requests carrying `suggestion`.

### 4.5 Save-time match ("Find on Google" becomes automatic) and the listing check (`googleCheckRules.ts` + `googleCheck.ts`)

- **Save** (`saveFromSearch`, `addByUrl` with `googlePlaceId`): a row/pick with `googlePlaceId` → `insertProspect` writes `google_place_id`, `google_listing = 'found'`, `google_match = 'auto'` (`'manual'` for Add by URL), `google_confirmed_at = now`. No Google call at save time. `addByUrl` with a `googlePlaceId` whose website already belongs to a prospect (`existing`): when that prospect has no `google_place_id`, attach it (`google_place_id`, `google_listing = 'found'`, `google_match = 'manual'`, `google_confirmed_at = now`) and return `{ …existing, existing: true, placeAttached: true }`; when it already has one, return `existing: true, placeAttached: false` and leave it alone.
- **Name from the site** (D3, §5.5): `addByUrl` without a name stores `name = domainKey` as today. After a successful crawl, `runAuditJob` sets `prospects.name` (and `name_key`) from the site — the JSON-LD `Organization`/`LocalBusiness` `name` when the extraction has one, else the `<title>` cut at the first ` | `, ` – ` or ` - `, trimmed, ≤ 120 chars — **only when** `name` still equals `domain_key`; a name Radu typed is never touched.
- **The decision table** lives in `googleCheckRules.ts` (pure, relative imports, no DB — the `searchRules.ts`/`searchRepair.ts` split): `decide({ on, prospect: { placeId, listing, match, checkedAt, hasLocation }, latestSignals: { fetchedAt } | null, now }) → "passthrough" | "skip_no_location" | "match" | "signals" | "reuse"` and `applyMatch({ hit: string | null, now }) → { columns: Partial<{ google_place_id; google_listing; google_match; google_checked_at; google_confirmed_at }> }`; `googleCheck.ts` (I/O) runs the decision with `googleFetch` and `enquiriesDb`. Rules:
  1. Google off → `passthrough`: `{ status: prospect.google_listing, signals: null }` (today's behaviour: `found`/`not_found` only by manual confirmation, `unverified` → not measured).
  2. No `place_id` and `google_match ≠ 'manual'` and (`google_checked_at` null or older than 30 days) → `match`: one Text Search Pro (`matchSearchBody`, `google_search` pool) → `matchPlace` over the ≤ 5 candidates with the prospect as the row (name key, coordinates → 300 m, else postcode) → hit: `google_place_id`, `google_listing = 'found'`, `google_match = 'auto'`, `google_confirmed_at = now`; **miss: `google_listing` stays `'unverified'`**, `google_match = 'auto'`, `google_checked_at = now` (the check stays unmeasured with `reason: "no_match"`; the prospect page says *No match found automatically — match manually or mark not found*; the next automatic attempt is 30 days later). A prospect with **neither coordinates nor a postcode** (plain Add by URL) → `skip_no_location`: no request, no count, `reason: "no_location"`, nothing written. `google_match = 'manual'` (Radu confirmed or rejected) is never overridden automatically — only a manual *Not this listing* produces `not_found`.
  3. `place_id` present → `reuse` the latest audit's `google_listing.details` when its `fetchedAt` is < 30 days old, else `signals`: Details **Enterprise** (`google_details_enterprise` pool, cap 900) → `signalsOf`; on `allowance`/`busy`/`timeout` → `{ status: "found", signals: null, reason }` (no retry inside the job; the job never fails because of Google). There is no `fresh` job flag: **Check again** (inline, below) is always fresh, and a signals set older than 30 days is refreshed by any audit.
  4. `runChecks({ …, google })`.
- **Google-only audits**: every prospect saved while Google is on gets an audit job (today: only rows with a website). `enqueueAudit(id, { priority? })` (dedupe key unchanged): `saveFromSearch` and `addByUrl` enqueue with `priority: 8` for rows **without a website** so they never delay real website audits (jobs are claimed by `priority, id`). No-website audits do `noSiteChecks` + the Google check only (no crawl, no PageSpeed) and draw on their **own daily allowance** `GOOGLE_CHECK_DAILY_CAP` (default 30, `api_usage` provider `google_check_day`, UTC day like the other daily counters) instead of `AUDIT_DAILY_CAP`; over it → `RescheduleJob` to 06:00 Europe/Paris like the audit cap. `auditNext` extends its clause to `(website IS NOT NULL OR (website IS NULL AND google_place_id IS NOT NULL))` when `googlePlacesOn()`, so *Audit next 20* also refreshes Google-only prospects.
- `POST /api/admin/prospects/[id]/google` (rewritten; `rateLimit("google-check:" + ip, 30, 10 min)`; never `retry`): `{ action: "check" }` → steps 2–3 inline, always fresh (≤ 8 s), returns `{ ok, prospect, signals, usage }`; `{ action: "find" }` → one Text Search Pro (`matchSearchBody`, `pageSize 5`, `google_search` pool) → `{ ok, candidates: GoogleCandidate[], usage }` (transient; shown only inside the open Google section, §5.6; no Details call of any tier); `{ action: "confirm", placeId }` → `google_match = 'manual'`, `found`, then `check`; `{ action: "reject" }` → `not_found`, `google_match = 'manual'`, `place_id NULL`, `google_checked_at = now`; `{ action: "clear" }` → `unverified`, `google_match NULL`, `google_checked_at NULL` (auto-match may run again). Errors: `501 google_off`, `429 google_monthly_cap { pool }`, `502 google_unavailable`, `502 google_refused`. `GET /api/admin/prospects/[id]` adds `googleCheckedAt`, `googleMatch`, `googleSignals` (from `audits.checks` of `latest_audit_id`, via `store.ts googleSignals(prospectId)`, which rebuilds `attributions` from the stored joined string).

### 4.6 Scoring (`checks.ts`, `score.ts`) and wording (`auditChecks.ts`, `findings.ts`)

`ChecksInput.google: { status: "unverified" | "found" | "not_found"; signals: GoogleSignals | null; reason?: GoogleListingReason }` replaces `googleListing`. `googleListing(google)`:

| Case | status | points | details |
|---|---|---|---|
| `unverified` (never checked, automatic miss, or no location) | not_measured | — | `{ listing: "unverified", reason?, checkedAt? }` — the UI says *Not checked yet* / *No match found automatically* / *Not checked — no address or coordinates to match on* |
| `not_found` (manual only) | fail | 0 | `{ listing: "not_found", checkedAt }` (flag `no-gbp`, unchanged) |
| `found`, signals null | not_measured | — | `{ listing: "found", reason }` — the UI says *Listing found — details not checked (allowance used up / Google did not answer)* |
| `found` + signals | pass ≥ 9 · partial 4–8 | 4 exists + 1 operational + 2 website on the listing + 1 hours + 1 reviews ≥ 5 + 1 photos ≥ 1 | `{ listing: "found", operational, websiteOnListing, hours, reviews, photos, fetchedAt, attributions }` — `attributions` stored as one string, names joined with `" · "` (`""` when none), because `CheckResult.details` holds primitives |

`makeCheck` gains an optional `points` override (clamped to the weight) so the sub-scores carry into `auditScore` unchanged; `score.test.ts` covers 10 → pass, 8 → partial (points 8, not 5), 4 → partial, manual not_found → fail + `no-gbp`, automatic miss → not_measured and no flag. **Grade cap**: `auditScore` sets `grade = "C"` whenever `flags` contains `no-site` (a no-website business with a perfect listing would otherwise read 50 = B); score number unchanged; tested. Report copy (`src/content/auditChecks.ts`, prospect-facing, FR/EN — status words only, D2): pass → *Votre fiche Google est en place et complète.* / *Your Google listing is in place and complete.* (the signals do not measure whether a listing is looked after, so no sentence says so); partial → unchanged: *Votre fiche Google existe, mais elle est incomplète.* / *Your Google listing exists, but it is incomplete.* (no list of what is missing); fail and not_measured unchanged (fail is reached only after Radu marked the listing not found). `findings.ts googleLine` unchanged (our status sentence only, never a quote from a listing nor a signal); `findings.test.ts` updated. Draft templates and the draft prompt mention the listing only through the existing `no-gbp` flag words — no change, and no per-signal words ever reach `findingsInPlainWords`.

### 4.7 Counters, Today, purge — the 30-day rule

- `api_usage` providers: `google_search`, `google_details_enterprise`, `google_details_other` (monthly, Pacific day keys, §4.2) and `google_check_day` (daily). `today.ts` `collectToday` adds `googleOn`, `googleKeyMissing`, `googleChecksMonth`/`googleChecksCap`, `googleSearchMonth`/`googleSearchCap`, `googleOtherMonth`/`googleOtherCap`. **Card contract** (the backend emits, `todaySummary.ts` renders, `today.test.ts` asserts the cards, `todaySummary.test.ts` the sentences, the digest mirrors the cards): when `googlePlacesOn()`, card `{ key: "google", title: "Google usage this month", value: "12 / 900", detail: "40 / 4,500 searches", tone }` (tone `warn` when any pool ≥ 90 %, `bad` at a cap, else `neutral`; the current *Google calls this month* card is replaced, and nothing is emitted when Google is off); when `GOOGLE_PLACES=on` and the key is missing, card `{ key: "google-key", title: "Google key", value: "missing", tone: "warn" }` instead. `todaySummary.ts`: `google` → *Google usage: 12 of 900 listing checks · 40 of 4,500 searches this month* (always shown when the card exists, even at 0); `google-key` → *Google is switched on, but its key is missing.* (tone `warn`).
- Nothing Google-derived needs refreshing on a timer because nothing beyond the exceptions is stored: `place_id` (kept), lat/lng only inside `api_cache["search:{id}"]` (24 h TTL < 30 days), signals as derived numbers with `fetchedAt`, attribution provider names. `scripts/digitalm-purge.js` gains **step 12 — Google**: `DELETE FROM api_cache WHERE provider LIKE 'google%'` (safety net; the client never writes any) and a count of `audits` whose `google_listing.details.fetchedAt` is older than 30 days reported as *Google signals older than 30 days: n (re-checked at the next audit)* — no deletion (they are our measurements, D2). The prospect page labels signals older than 30 days *checked 45 days ago — re-check* (§5.6); `Audit next 20` and **Check again** refresh them.

### 4.8 API contract (all under the existing auth/CSRF rules; error shape `{ ok:false, error, message? }`)

- `POST /api/admin/find` body adds `suggestion?: { placeId }` (§4.4; any other key in `suggestion` is ignored). `202` response carries `queryArea` (the derived query). New error `400 suggestion_unresolved` (empty `area` and Google could not resolve the id).
- `GET /api/admin/find?id=&after=&v=&pins=` — `SearchResultV2` adds `googlePins?: GooglePin[]` (present only when the Google phase ran; included in the response when `after` is absent/0 **or** `pins` (the `pinsVersion` the client holds) is absent or lower than `progress.google.pinsVersion` — otherwise omitted from the slice like rows), `progress.google?: GoogleProgress`, `progress.stage` may be `"google"`, rows may carry `googlePlaceId` and `onGoogle`.
- `POST /api/admin/find/continue` — unchanged (no Google-only continue; *Run again* covers a failed Google phase).
- `POST /api/admin/find/dismiss { searchId, key, undo? }` — `key` may be `google:<placeId>` (validated with `placeIdOk`, ≤ 79 chars total, `400 bad_key` otherwise); accepted when the pin exists in the cached result; stored in `searches.dismissed` like row keys.
- `POST /api/admin/find/save` — unchanged; saved rows carry their `googlePlaceId` into `prospects`.
- `POST /api/admin/prospects { url, name?, country, googlePlaceId? }` — Add by URL stores the place id (`google_match='manual'`, `found`); response adds `placeAttached?: boolean` for the `existing` case (§4.5); `400 bad_place_id` on a malformed id.
- `POST /api/admin/prospects/[id]/google` — §4.5 actions. `GET /api/admin/prospects/[id]` — §4.5 fields.
- Library exports (`index.ts`): `googlePlacesOn`, `googleDiscoveryOn`, `googleMapConfigured`, `googleUsage`, `GoogleError`, `GOOGLE_TYPES`.

Notes catalogue additions (`notes.ts`, tested for text and forbidden tokens — none may contain *tile* or *partial*): 17 `google_no_key` — "Google is switched on, but its key is missing." · 18 `google_discovery_off` — "Google is used for the map, place suggestions and listing checks, not yet to find businesses." · 19 `google_failed` — "Google did not answer, so nothing from Google is shown for this search." · 20 `google_refused` — "Google refused the request — the key's restrictions need a look." · 21 `google_allowance` — "The monthly Google search allowance is used up — Google was not asked for this search; what is left is reserved for listing checks." · 22 `google_incomplete` — "Google was asked for {done} of {total} parts of {area}; some businesses known only to Google may be missing." · 23 `google_outside_dropped` — "{n} Google results outside {area} were left out." · 24 `google_matched` — "{matched} also on Google · {only} only on Google." · 25 `google_all_parts` — "Google was asked about the whole of {area}, not only the parts this search covered."

### 4.9 Privacy notice, Art. 30 register, LIA (backend, one sentence each; gate L0 of §8.3)

The notice every prospect receives (`src/content/fr.ts` / `en.ts`, the prospecting body) currently says *nous ne collectons rien sur Google Maps* / *we collect nothing from Google Maps* and *we then keep only a listing identifier*; the Art. 30 register says the Maps Platform use is *désactivé / off*; the LIA lists "Google Places is switched on" as a re-assessment trigger. Under this contract, with D2 (default) we store derived signals per audit, the match step sends *"<name> <city>"* to Google (a sole trader's name is personal data), and with D1 on businesses are discovered through Google. Edits, all backend-owned, all committed **before** `GOOGLE_PLACES=on` reaches prod:

- `fr.ts` body — replace the recipients clause with: *Google Maps Platform, si nous vérifions votre fiche Google : nous conservons un identifiant de fiche et le résultat de cette vérification (fiche trouvée, site, horaires, nombre d'avis et de photos), jamais le contenu de la fiche.* `en.ts`: *Google Maps Platform, if we check your Google listing: we keep a listing identifier and the result of that check (listing found, website, hours, number of reviews and photos), never the listing's content.* While `GOOGLE_PLACES_DISCOVERY=off` the sources sentence keeps *rien sur Google Maps* / *nothing from Google Maps* — it is still true for discovery; the day D1 is switched on, the same commit lists *Google Places (Google Maps Platform)* among the sources and drops that clause. (`src/content/outreach.ts` already names *votre fiche Google (Google Maps Platform)* — unchanged.)
- `docs/leadgen-registre-art30.md` — *Sources* rows: *Google Maps Platform : identifiant de fiche et résultat dérivé de la vérification (fiche trouvée, site, horaires, nombre d'avis et de photos) ; à l'activation de la découverte, fiches Google Places dans la zone recherchée* (EN mirror); *Destinataires/Recipients*: Google Maps Platform (listing identifier, derived check result; the business name and town are sent for the match); *Transferts/Transfers*: unchanged (Google already listed). Drop *(désactivé)* / *(off)*.
- `docs/leadgen-lia.md` — §2 "not collected" bullet: *nothing is scraped from Google Maps; when the Google check is on, one listing identifier and a derived result are kept per audit, and the business name and town are sent to Google to find the listing*; §3 processors bullet: same words; §5: the re-assessment is **done** — add the dated line *Google Places switched on (date): re-assessed, no change to the basis; derived result kept per audit (30-day refresh), discovery per D1* signed by Radu. That signature is gate **L0**.

---

## 5. Frontend

### 5.1 Loading Maps JavaScript, custom elements, and the CSP

`googleMaps.ts` (client, singleton): `loadGoogleMaps(): Promise<typeof google.maps>` injects once `<script src="https://maps.googleapis.com/maps/api/js?key=<NEXT_PUBLIC_GOOGLE_MAPS_KEY>&v=weekly&loading=async&language=en&region=FR&callback=__dmGmapsReady">`, resolves on the callback, rejects after 10 s or on `error`; callers then `await google.maps.importLibrary("maps" | "marker" | "places")`. `googleMapsPlaceUrl(placeId)` → `https://www.google.com/maps/place/?q=place_id:<encoded>` (null when `placeIdOk` fails). Nothing loads until a component that needs it mounts (the finder page with the Google map; the Area box; a *Show the Google listing* panel when opened). A module counter `window.__dmGmapLoads` is incremented every time `new google.maps.Map()` runs (e2e G8 asserts it stays ≤ 1 across searches).

**UI Kit elements are created imperatively, never written as JSX.** `@types/react` has no catch-all for hyphenated tag names (TS2339 under `strict`) and `@types/google.maps@3.66` ships the element classes but no JSX declarations; more importantly `FindForm`/`BusinessCard` are server-rendered client trees, and an unknown element in the SSR output hydrates before the library exists, while the autocomplete element's value lives in a shadow DOM with no documented property. So `createPlacesElement(tag, attrs, children?)` (in `googleMaps.ts`) does `document.createElement(tag)` + attribute assignment inside a `useEffect` on a container `ref`, **after** `await importLibrary("places")`, and removes the node on cleanup. No `declare global` JSX augmentation anywhere.

`next.config.mjs` — one added `headers()` entry after the global one, only when `process.env.NEXT_PUBLIC_GOOGLE_MAPS_KEY` is set at build time (the public site's CSP is untouched; Next applies the last matching entry for the same header key):

```
source: "/admin/:path*"
Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://challenges.cloudflare.com https://maps.googleapis.com https://*.gstatic.com https://*.google.com blob:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https: blob:; font-src 'self' data: https://fonts.gstatic.com; connect-src 'self' https://challenges.cloudflare.com https://maps.googleapis.com https://*.googleapis.com https://*.gstatic.com https://*.google.com data: blob:; frame-src 'self' https://challenges.cloudflare.com https://*.google.com; worker-src 'self' blob:; frame-ancestors 'self'; base-uri 'self'; form-action 'self'
```

`X-Robots-Tag` / `Cache-Control` of that entry stay as they are. `NEXT_PUBLIC_*` values are build-time: staging needs them in `.env.local` before `npm run build`.

### 5.2 `GoogleFindMap.tsx` — same props as `FindMap`, same behaviour, Google underneath (D4)

`FindWorkspace` picks the map: `googleMap` prop (from `googleMapConfigured()`) → `dynamic(() => import("./GoogleFindMap"), { ssr: false })`, else `FindMap` (Leaflet). `GoogleFindMap` renders the Leaflet `FindMap` itself when `loadGoogleMaps()` rejects, with a one-line note above the map (`GOOGLE_TEXT.mapFailed`: *The Google map could not be loaded — showing OpenStreetMap instead.*), so a wrong key or a blocked script never leaves the page without a map.

- **One Map instance per `FindWorkspace` mount, reused across searches and areas.** `new google.maps.Map(el, { mapId: NEXT_PUBLIC_GOOGLE_MAP_ID, center, zoom, mapTypeControl: false, streetViewControl: false, fullscreenControl: false, gestureHandling: "greedy", clickableIcons: false })` runs once (one Dynamic Maps load); a new search, a new area or a `?search=` navigation updates centre, outline and markers in place — the component is **never keyed on the search id**. The effect guards against React StrictMode's double invocation by keeping the instance on the container (`el.__dmMap`) and reusing it. A breakpoint change still remounts the map pane (desktop and phone place `mapPane` in different containers) — accepted and noted in §11. The `mini` prop is **not** implemented here — `MiniMap` stays Leaflet everywhere (no map loads spent on one pin, and the prospect page's Google section rule of §3.4 stays simple).
- Pins: `AdvancedMarkerElement` with `content` = a `<div class="dm-gpin …">` carrying the pin vocabulary as classes (`dm-gpin-website`, `-nowebsite`, `-saved`, `-hidden`, `-outside`, `-selected`, `-hovered`, `-google` for Google-only pins in **blue** `#3B82F6`; a Google-only pin with `saved` set takes `-saved` green), `title` = the row's name (empty for Google-only pins), `gmpClickable: true`, `gmp-click` → `onSelect(key)`. Pin keys for Google-only pins are `google:<placeId>`.
- Clustering: `@googlemaps/markerclusterer` (`MarkerClusterer({ map, markers, algorithmOptions: { radius: 56, maxZoom: 17 }, renderer })`) with a renderer drawing the same numbered disc as Leaflet (`dm-gcluster`, sizes from `mapCluster.ts clusterSize`); the selected pin is kept out of the clusterer (added to the map directly). `window.__dmFindPins = { total, visible, clustered, selectedKey }` after every render; `window.__dmFindProject(key)` uses a hidden `OverlayView` (`dmProjector`) → `getProjection().fromLatLngToContainerPixel(latLng)`; cluster discs are DOM elements with class `dm-gcluster` (the e2e accepts either class).
- Outline: `google.maps.Polygon` per polygon (outer ring + holes as `paths`), `strokeOpacity 0`, `fillColor #F15A24`, `fillOpacity 0.06`, `clickable: false`, plus one `Polyline` per ring with the documented dash pattern (`icons: [{ icon: { path: "M 0,-1 0,1", strokeOpacity: 1, scale: 2 }, offset: "0", repeat: "12px" }]`, `strokeColor #F15A24`); `polygonApprox` outlines get a `title` tooltip element *approximate outline*; `fitBounds(bbox, 24)` on a new area or `fitSignal`.
- Sync: `hoverKey`/`selectedKey` restyle the marker content; click row → `panTo` only when the pin is outside the bounds padded by 10 % (never a zoom change); `onViewport` on `idle` (debounced 80 ms) with `getBounds()`; chips filter markers and rows together; *List follows the map* unchanged. Unit dots: small `AdvancedMarkerElement`s with `dm-unit*` classes. Progress bar overlay and the legend button: same DOM as today.
- Attribution for our data on Google's map: a small `© OpenStreetMap contributors` line (link to the copyright page) at the map's bottom-left (`dm-osm-attr`), because on the phone the map fills the viewport and the list footer is off-screen. Google's own attribution is the map's.

### 5.3 The Area box with suggestions (`GoogleAreaInput.tsx`, D5)

When `googleOn`, `AreaInput` renders `GoogleAreaInput`, which starts as the plain `Field` and swaps in the element only once a `ready` state is set from a `useEffect` (`await loadGoogleMaps(); await importLibrary("places")`) — so SSR and hydration see an ordinary input, and the Field returns whenever the script fails, Google is off, or the ambiguity chooser (candidates) is shown. The element is a `<gmp-basic-place-autocomplete>` created with `createPlacesElement` (§5.1): `includedPrimaryTypes = ["(regions)"]`, `requestedLanguage = "en"`, `placeholder = FIND_TEXT.areaPlaceholder`, styled through its CSS custom properties to the admin field look (15–16 px, `border-line`, `bg-surface`); labelled by the existing *Area* `<label for>`; the element's own list is keyboard-navigable (arrow keys, Enter, Esc).

- **Typed text**: the element's value is not readable, so the component listens to `input` events (composed, they cross the shadow root) and keeps the last text in React state (`lastTyped`), mirrored into `form.area` — the phone's collapsed bar and the *Enter without picking* path post that text as today. When no `input` event ever arrives (an element build that does not compose them), the plain `Field` is rendered instead.
- **Pick**: `gmp-select` → `onSuggestion({ placeId: event.place.id })` — the payload carries the id only (the event's `Place` holds nothing else) — and the workspace posts `{ area: lastTyped, suggestion: { placeId }, category, … }`. The element clears its box on selection; on the `202` the component sets `form.area = response.queryArea` (*Ariège, France*) and shows it in the box. `400 suggestion_unresolved` → the message under the field, focus back in the box.
- Sessions are billed per selection by the UI Kit (10,000 free); no session token code on our side.

### 5.4 List, chips, legend, progress

- `FindList` header: the sources sentence gains *· 201 also on Google · 23 only on Google* when `progress.google` exists, followed by the text-form attribution (`GoogleAttribution text` — the one permitted use of the text, §3.5); chip **Only on Google (n)** (`Chip = … | "google_only"`, primary row, **on by default** when pins exist — it toggles the blue pins, it filters no rows); `counts.google_only = googlePins.filter(!hidden).length`.
- `Legend`: *Blue pin — on Google only (open it to see the listing)*; the *?* panel gains one line *Google data is shown by Google's own panels* with the `GoogleAttribution` logo (outlined variant — the panel sits over the map).
- `FindProgress`: stage `google` → *Asking Google — {done} of {total} parts of {area}* (`GOOGLE_TEXT.asking`; never the word *tiles*); a failed Google phase shows note #19–#21 as a muted line under the finished status with the *Run again* link (never an amber banner — the search is complete without Google).
- `progressModel.ts`: the Google stage takes a 10 % share when present.

### 5.5 Business card — Google sections, and the Google-only card

- Rows with `googlePlaceId`: in **Where it came from**, one line *Also on Google* + `GoogleAttribution` (logo) + **View on Google Maps** (`googleMapsPlaceUrl`, `rel="noopener noreferrer nofollow"`, new tab); under it a `<details>` **Show the Google listing** (`data-testid="card-google-listing"`) whose body is `GoogleListingPanel` in its **compact** variant (§5.7). The plain *Open in Google Maps* coordinate link of today stays for every pinned row.
- `GoogleOnlyCard.tsx` (`role="dialog"`, `data-testid="google-only-card"`, opened by `select("google:<placeId>")`): title *On Google only* (`h2`, 22 px), one muted sentence (`GOOGLE_TEXT.googleOnlyHint`: *Google knows this business; OpenStreetMap and the French company register do not. Open the listing to find its website, then add the site — the name is taken from the site.*), the `GoogleListingPanel` in its **full** variant (loaded at once — this card exists to show it, and the full element is the one that shows the website), **View on Google Maps**, then actions: **Add by its website** (`data-testid="google-add-by-url"`; opens the Add-by-URL form in the card with `country = area.countryCode`, a hidden `googlePlaceId` and **no name field** — Radu types the URL he reads in Google's panel (it goes through `coerceHttpUrl` as today) and the name comes from the site (§4.5); success → toast *Saved as PR-… · audit queued*, and the pin turns green on the next poll through `pin.saved`; `existing` with `placeAttached` → toast *Already saved as PR-… · Google listing attached*; `existing` without → *Already saved as PR-…*) and **Not this one** (dismiss key `google:<placeId>`, Undo toast). A listing that shows no website cannot be saved from the finder (there is nothing to audit) — the pin stays blue. Esc/Back as the business card. When the Maps script failed, the panel area says *Google's listing panel could not be loaded* and the link remains.
- A pin with `saved` set opens the same card with the title *On Google only · saved as {reference}* and a link to the prospect instead of the Add form.

### 5.6 Prospect page — `GoogleBlock.tsx` rewritten

Hidden while Google is off (as today). Placed **after** `AuditBlock` (below the fold; the page's block order is a frontend edit, §2.2), as a `<details data-testid="google-block">` collapsed by default with the summary *Google listing · {status word}*; while open it sets `data-google-open` on `document.documentElement`, and `MiniMap`'s wrapper (`data-testid="mini-map"`, in `MiniMap.tsx`) carries `[html[data-google-open]_&]:hidden` (Tailwind arbitrary variant) — rule §3.4.

Inside: status badge (no attribution — our result) — *Listing found · looks maintained* (pass) / *Listing found · looks unmaintained* (partial) / *Listing found — details not checked* (found, no signals) / *No listing found* (manual) / *No match found automatically — match manually or mark not found* (`reason: no_match`) / *Not checked — no address or coordinates to match on* (`no_location`) / *Not checked yet*; a line *Checked {relative} · matched automatically | confirmed by you* (`googleCheckedAt`, `googleMatch`), *checked 45 days ago — re-check* past 30 days; **the signals as words, rendered here and nowhere else** (`GOOGLE_TEXT.signal*`: *open according to Google* / *marked closed on Google*, *website on the listing* / *no website on the listing*, *opening hours filled in* / *no opening hours*, *{n} reviews*, *{n} photos*) with `GoogleAttribution` (logo) and, when `googleSignals.attributions` is non-empty, *Data: {names}* from the stored value; **View on Google Maps**; a nested `<details>` **Show the Google listing** → `GoogleListingPanel` (full variant); buttons **Check again** (`action: "check"`), **Match manually** (`action: "find"` → the ≤ 5 candidates as *{name} — {addressLine} · {distance} m* each with its own *Data: {names}* when present and **Use** → `confirm`; rendered only here, inside the open section), **Not this listing** (`reject`), **Clear** (`clear`, shown when not `unverified`). Toasts in sentences: `google_off` → *Google is switched off for now*; `google_monthly_cap` → *The monthly Google allowance is used up — try again next month.*; `google_unavailable` → *Google did not answer. Try again later.*; `google_refused` → *Google refused the request — the key's restrictions need a look.*

`AuditBlock`'s detail line for `google_listing` shows **status words only** from `details.listing` + `reason` (*Listing found · looks maintained* / *… looks unmaintained* / *Listing found — details not checked* / *No listing found* / *No match found automatically* / *Not checked yet*; never the raw token, never a signal word or count) — the audit block sits on the same page as the visible mini map, so the derived words stay inside the collapsed Google section (§3.4); no attribution on that line.

### 5.7 `GoogleListingPanel.tsx`, `GoogleAttribution.tsx`, wording

- `GoogleListingPanel({ placeId, variant: "full" | "compact" })`: `await loadGoogleMaps(); await importLibrary("places")`, then `createPlacesElement`. **Full** (Google-only card, prospect page): `<gmp-place-details>` with `<gmp-place-details-place-request place="<id>">` and an explicit `<gmp-place-content-config>` holding `gmp-place-address`, `gmp-place-website`, `gmp-place-phone-number`, `gmp-place-opening-hours`, `gmp-place-rating`, `gmp-place-media`, `gmp-place-attribution` — the compact element does not support website, phone or opening hours (Appendix A), and the website is what *Add by its website* needs. **Compact** (business card): `<gmp-place-details-compact orientation="vertical">` with the place request and `<gmp-place-standard-content>`. Both are the same *Places UI Kit Query* SKU on Maps JavaScript. Google renders name, address, rating, open state, website, phone, hours, a photo with its author credit and the Google attribution — nothing of it passes through our code; `gmp-load` → nothing to read but `place.id`; `gmp-error` → *Google's listing panel could not be loaded*. One UI Kit query per mount; a panel inside a `<details>` mounts only when it opens.
- `GoogleAttribution({ text?: boolean; onMap?: boolean })`: `<span class="dm-google-attr" translate="no">` with `<img src="/brand/google-maps-logo.svg" alt="Google Maps" height=16>` (the `-outlined` file when `onMap`), 16–19 px high, 10 px clear space; `text` renders *Google Maps* instead and is used only by the list-header count. No existence check of any kind.
- `wording.ts` `GOOGLE_TEXT` holds every sentence above (`attribution: "Google Maps"`, `mapFailed`, `onlyOnGoogle`, `onlyOnGoogleChip: "Only on Google ({n})"`, `alsoOnGoogle`, `viewOnGoogleMaps`, `showListing`, `googleOnlyHint`, `addByWebsite`, `panelFailed`, `savedAs`, `dataFrom: "Data: {names}"`, status words, `checkedLine`, `recheck`, signal words, button labels, toasts, `asking: "Asking Google — {done} of {total} parts of {area}"`, `runAgainHint`); `FORBIDDEN_TOKENS` adds `/\bclaimed\b/i`, `/\bverified\b/i`, `/Google My Business/i`, `/\bGMB\b/`; `wording.test.ts` walks `GOOGLE_TEXT` too — with the existing `/\btiles?\b/i` and `/\bpartial\b/i` in force, so no sentence may use either word.

### 5.8 Today

`TodayCards`/`todaySummary.ts`: the two cards of §4.7 (`google`, `google-key`) render the two housekeeping sentences; the current *Google usage: 0 of 900 this month* suppression rule is removed (the card only exists when Google is on); `todaySummary.test.ts` asserts both sentences from fixture cards.

---

## 6. Environment — `.env.example` block (backend replaces today's three `GOOGLE_*` lines under `# @@crm:finder`)

```
# ---- Google (docs/finder-google-spec.md) ------------------------------------
# on|off — Google in the CRM: the finder map (Google Maps), place suggestions in the Area box, the
# Google-listing check of saved prospects and the Google section on the prospect page.
# off = the OpenStreetMap map and no Google call anywhere (today's behaviour).
GOOGLE_PLACES=off
# on|off — also ask Google Places when finding businesses (§0, decision D1). off = searches never call Google;
# everything else above still works.
GOOGLE_PLACES_DISCOVERY=off
# Server-side Places API (New) key. Restrict it in Cloud Console to this server's egress IP and to the
# "Places API (New)" API only. Never sent to a browser, never logged.
GOOGLE_PLACES_KEY=
# Monthly hard stops, counted per Google SKU family in api_usage on the Pacific calendar day (Google's
# free tier resets on the 1st at midnight Pacific); 90 % of the free allowances — the 10 % is headroom for
# billing-window skew and for requests counted but not billed:
# listing checks = Place Details Enterprise (1,000 free) · searches = Text/Nearby Search Pro (5,000 free;
# discovery stops 500 short of this cap, keeping them for listing checks) · other details = Place Details
# Essentials (area suggestions; 10,000 free).
GOOGLE_PLACES_MONTHLY_CAP=900
GOOGLE_SEARCH_MONTHLY_CAP=4500
GOOGLE_DETAILS_MONTHLY_CAP=4500
# Google requests one search may spend on discovery (each answers at most 20 places).
GOOGLE_SEARCH_MAX_REQUESTS=30
# Listing checks per day for prospects without a website (their audits have no crawl and draw on this
# allowance instead of AUDIT_DAILY_CAP).
GOOGLE_CHECK_DAILY_CAP=30
# Browser key (build-time — rebuild after changing). Restrict it to HTTP referrers https://digitalm.eu/* and
# https://d3v.digitalm.eu/* and to the "Maps JavaScript API" and "Places UI Kit" APIs. Blank = the
# OpenStreetMap map even with GOOGLE_PLACES=on.
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
# Map ID (Cloud Console → Map Management), required for the markers on the Google map. DEMO_MAP_ID works on staging.
NEXT_PUBLIC_GOOGLE_MAP_ID=
```

Keys: `GOOGLE_PLACES`, `GOOGLE_PLACES_DISCOVERY`, `GOOGLE_PLACES_KEY`, `GOOGLE_PLACES_MONTHLY_CAP`, `GOOGLE_SEARCH_MONTHLY_CAP`, `GOOGLE_DETAILS_MONTHLY_CAP`, `GOOGLE_SEARCH_MAX_REQUESTS`, `GOOGLE_CHECK_DAILY_CAP`, `NEXT_PUBLIC_GOOGLE_MAPS_KEY`, `NEXT_PUBLIC_GOOGLE_MAP_ID`.

Cloud Console side (Radu, §8.3 L1): two keys, one project, billing on, APIs enabled: *Places API (New)*, *Maps JavaScript API*, *Places UI Kit*. **Per-day quota caps** (APIs & Services → Quotas — the only enforceable hard stop for the browser key, whose referrer restriction any non-browser client can spoof, and a backstop for the server pools if a bug loops): *Maps JavaScript API* map loads/day **500**; *Places UI Kit* requests/day **1,000**; *Places API (New)* requests/day **400** (above the CRM's daily need — ≤ 30 discovery + ≤ 30 listing checks + suggestions — and far under a runaway). A budget alert at **€5** stays as the secondary signal (it only emails after spend).

---

## 7. Security

- **Server key**: read only in `src/lib/discover/google.ts`, sent as `x-goog-api-key`, never in a URL, never logged (`fetchJson` logs nothing; `google.ts` logs error codes only). Added to the "never log" list of `leadgen-build-spec.md` §10 (already there). IP-restricted to the VPS egress address + API-restricted to Places API (New) — a leaked key is useless elsewhere; the per-day quota (§6) bounds what a leaked key could spend.
- **Browser key**: `NEXT_PUBLIC_GOOGLE_MAPS_KEY` is public by nature; referrer-restricted to the two hosts and API-restricted to Maps JavaScript API + Places UI Kit (not Places API (New) — the browser never calls the web service); the per-day quotas of §6 are the real spend limit. Test §3.7: no `GOOGLE_PLACES_KEY` under `src/components`.
- **SSRF unchanged**: Google is a fixed origin in `HOSTS`; no URL from data is ever fetched (`googleMapsUri` is not requested — the *View on Google Maps* link is built from the validated place id; `websiteUri` is never fetched by us — D3 — and when Radu types it into the website field it goes through the existing `coerceHttpUrl` + audit SSRF rules).
- **Rate limits**: `google-suggest` 60 / 10 min per IP on `POST /api/admin/find` with a `suggestion`; `google-check` 30 / 10 min per IP on `/prospects/[id]/google`; the monthly pools (reserved before the socket) and the daily Google-only allowance stop everything else. All routes stay behind `guardAdminPost`/`requireAdminApi`.
- **Input validation**: `placeIdOk` (`{10,72}`) on every place id from a client or a fixture; dismiss keys `google:<id>` validated; candidate lists ≤ 5, strings ≤ 200 chars, attribution names ≤ 5 × 80 chars, rendered as React text nodes.
- **CSP**: admin pages only (§5.1); the public site's CSP is unchanged. `frame-src *.google.com` exists only because the Maps script may open Google's own frames (Street View is disabled; the UI Kit needs none today) — dropped if the live checklist shows no frame is requested.
- **Data minimisation** as in §3: no names/addresses/reviews/photos from Google in SQLite or logs; matching happens in memory and the response is discarded; fixtures are synthetic; the name of a prospect is never typed from a Google panel.

---

## 8. Tests

### 8.1 Fixtures (`src/lib/discover/fixtures/google/`, synthetic — made-up names and ids in the documented shapes, never captured live content)

`text-search.tile1.page1.json` (20 places, `nextPageToken`), `text-search.tile1.page2.json` (12 places, no token) — including one place with `country = AD`, one with postcode `66xxx`, one without address components, one with `attributions: [{ provider: "Example Data Co" }]`; `text-search.saturated.json` (20 + token, three times); `text-search.match.json` (5 candidates for the manual-match and save-time-match tests, one 40 m from the prospect); `nearby-search.json`; `details.enterprise.maintained.json` (OPERATIONAL, website, hours with periods, rating 4.5, 37 reviews, 3 photos, `attributions: []`), `details.enterprise.unmaintained.json` (OPERATIONAL, no website, no hours, 2 reviews, 0 photos, one attribution), `details.enterprise.closed.json` (CLOSED_PERMANENTLY), `details.essentials.ariege.json` (types `administrative_area_level_2, political`, components Ariège / Occitanie / France, location, viewport), `details.essentials.cambridge-uk.json`, `error.403.json`, `error.429.json`; `search-ariege.sample.json` gains `googlePins` (3, one with `saved`) and `progress.google` (with `pinsVersion`) in its `done` result. Place ids in fixtures are 27 chars; one deliberately 72 chars for the length tests. No `details.pro.*` fixture.

### 8.2 Unit (`node:test`, pure modules, relative `.ts` imports)

- `googleRequests.test.ts`: the three masks vs the tier table (no higher-tier field; `nextPageToken` allowed; no `MASK_DETAILS_PRO` export); `textSearchBody` (rectangle low/high order, `strictTypeFiltering` only with a type, `pageSize 20`, page token carried, language by country, `bias` circle with the radius clamp and exclusive with `rect`); `nearbySearchBody` radius clamp; `matchSearchBody` bias circle vs region only; `parseSearch` keeps only our fields and drops unknown ones, carries `attributions`; `signalsOf` on the three Enterprise fixtures → `{10, 5, 4}`-point inputs and **only** booleans, numbers, `fetchedAt` and `attributions` (an array of strings, ≤ 5, ≤ 80 chars each); `parseDetailsEssentials` → kind hints and queries for Ariège and Cambridge; `candidatesOf`; `placeIdOk` (27-char ok, 72 ok, 73 refused, 9 refused); `GOOGLE_TYPES` covers every `CATEGORIES` key and every `includedType` is in the Table A list embedded in the test.
- `googleTiles.test.ts`: `n` from expected (292 → 8 → 3×3 grid, cells outside the Ariège fixture polygon dropped — the Andorra corner cell goes), one tile when unknown, ordering from the centre, `ran` boxes keep only intersecting cells (a tile-mode plan and a **children-mode plan** whose units carry bboxes), `splitTile` quadrants and depth cap, budget stop with the skipped count, circle + null type → one Text Search body with `bias`, circle + Table A type → Nearby.
- `googleInside.test.ts`: department 09 (postcode `09000` yes, `66500` no, `AD` no, none → approx, `2A` → `20`), region Occitanie, town Foix (postcode set, locality), country Andorra, non-FR Cambridge (`adminLevel2` Cambridgeshire), place circle → yes.
- `googleMatch.test.ts`: same name 40 m → match; same name 400 m → none; "Le Phoebus Restaurant" vs "Le Phoebus" (stripped word) → match; different name 10 m → none; register row without coordinates + same postcode → match; nearest of two; a place matched to at most one row.
- `googleCheckRules.test.ts` (pure decision table): off → `passthrough`; no id + unverified + never checked → `match`; no id + `auto` miss 10 days ago → `reuse`-nothing (no request: `passthrough` with `reason: no_match`); `auto` miss 31 days ago → `match`; `manual` never re-matched; neither coordinates nor postcode → `skip_no_location`; id + signals 10 days old → `reuse`; 31 days → `signals`; `applyMatch` hit/miss column sets (miss leaves `google_listing` untouched); `allowance` → found without signals; the decision never throws on a `GoogleError` code.
- `checks.test.ts` / `score.test.ts`: §4.6 table; automatic miss → not_measured, no `no-gbp`; manual not_found → fail + `no-gbp`; points 8 → partial keeps 8; grade cap C with `no-site`; `details.attributions` is a string; flags unchanged otherwise. `findings.test.ts`: the new pass copy FR/EN, partial unchanged, no signal word in any sentence. `notes.test.ts`: codes 17–25, `FORBIDDEN_TOKENS.length === 13`, no text contains *tile* or *partial*. `today.test.ts`: the `google` and `google-key` cards. Static tests of §3.1/§3.3/§3.7.
- Frontend: `wording.test.ts` (`GOOGLE_TEXT`, new forbidden tokens, the existing *tile*/*partial* regexes), `format.test.ts` (`googleMapsPlaceUrl` valid/invalid ids), `todaySummary.test.ts` (the two sentences), `progressModel.test.ts` (Google stage share), `mapCluster.test.ts` unchanged (the clusterer is Google's; our disc sizing is still tested).
- Gates: `npm test` green; `NODE_OPTIONS=--max-old-space-size=1536 npx tsc --noEmit` clean (no JSX custom-element declarations needed — §5.1); no `.env*` in `git log main..feat/finder-google`.

### 8.3 Live checklist (once Radu's keys exist; integrator, on staging with `--db` staging)

- **L0 Records.** The §4.9 edits (privacy notice FR/EN, Art. 30 rows, LIA re-assessment line signed by Radu) are committed and deployed **before** `GOOGLE_PLACES=on` is set in prod `.env.local`; staging may run with Google on before that for L1–L10.
- **L1 Keys and quotas.** Server key: IP + API (Places API (New)) restricted; `curl` Text Search with `MASK_SEARCH_PRO` → 200; with a mask containing `websiteUri` the client refuses locally (`mask_tier`) before any call. Browser key: referrer + API (Maps JS, Places UI Kit) restricted; loading the finder from an unlisted origin shows the Leaflet fallback and the note. Quotas page shows the three per-day caps of §6 and the €5 budget alert exists.
- **L2 SKUs.** After one Ariège/restaurant search with discovery on, one prospect audit, one suggestion pick and one manual match, the Cloud Console *APIs & Services → Metrics* shows only: Text Search Pro (≤ 30 + 1 + 1), Place Details Enterprise (1), Place Details Essentials (1), Dynamic Maps (page opens), Places UI Kit Query (panels opened), Places UI Kit Autocomplete Per Session (1). **No Place Details Pro**, no Enterprise + Atmosphere, no Photos.
- **L3 Autocomplete element.** Typing *arie* lists suggestions; Enter on *Ariège, France* starts a department search with no chooser and the box reads *Ariège, France* afterwards; the payload of `gmp-select` is logged once in the console (`place.id` present; Appendix C notes whether any other field was populated) then the log line is removed; `input` events are observed from the element (else the plain Field fallback is in force — note it).
- **L4 Map.** Google map with the outline, ≥ 250 pins for Ariège, clusters at zoom 9, hover/click sync, `__dmFindPins.total ≥ 250`, `© OpenStreetMap contributors` visible on the phone layout; `__dmGmapLoads === 1` after a second search on the same page.
- **L5 Nothing stored.** The SQL of §3.1 returns 0 after L2; `api_usage` has `google_search`, `google_details_enterprise`, `google_details_other` rows on the Pacific day; `staging.log` contains no place name and no key.
- **L6 Discovery.** `googlePins` non-empty, no pin outside the department by eye (blue pins in Andorra = a bug in `googleInside`); `note google_matched` counts plausible (matched ≥ 60 % of OSM rows for restaurants).
- **L7 Audit.** A saved Foix prospect audited → `google_listing` measured with signals; the report line reads *Your Google listing is in place and complete* (or the partial sentence) and nothing else about the listing; the prospect page section shows the words, *Data: …* when Google named a provider, and the full panel with the website visible; the mini map disappears while the section is open; `AuditBlock` shows the status words only.
- **L8 Caps.** With `GOOGLE_PLACES_MONTHLY_CAP=0` on staging, **Check again** → 429 and the sentence; a search with `GOOGLE_SEARCH_MONTHLY_CAP=0` finishes `done` with note #21; with `GOOGLE_SEARCH_MONTHLY_CAP=510` and ≥ 10 searches already counted, discovery stops (#21) while **Match manually** still works; restore the values.
- **L9 Words.** `scripts/e2e/finder.cjs` G-items (§9) green; no forbidden token in any admin page.
- **L10 Billing.** 24 h later the billing report shows €0.00 for the day.

---

## 9. Acceptance (API with the staging cookie; e2e in `scripts/e2e/finder.cjs`, items G1–G9; all skipped with a printed *skipped — Google off* when `GOOGLE_PLACES` is off)

- **G1 — Off means unchanged.** With `GOOGLE_PLACES=off`: `/admin/find` renders one `.leaflet-container`, the Area box is a plain input, `GET ?id` has **no `googlePins` key** and no `progress.google`, the prospect page has no `[data-testid=google-block]`, and every finder-ux item A1–A22 still passes.
- **G2 — Suggestion pick.** `POST /api/admin/find { area: "arie", category: "restaurant", suggestion: { placeId: <Ariège id from L3> } }` → `202` with `area.kind = "department"`, `area.osmRelationId = 7439`, `queryArea` containing *Ariège*; `api_usage.google_details_other` **+1, always** (the payload carries the id only); the same with `area: ""` → `202` too; with a malformed `placeId` → `400 bad_suggestion`; with `area: ""` and `GOOGLE_DETAILS_MONTHLY_CAP=0` → `400 suggestion_unresolved`; with `area: "arie"` and the cap at 0 → resolves from `area` as today; with Google off → the request resolves from `area` as today.
- **G3 — Discovery payload.** With `GOOGLE_PLACES_DISCOVERY=on`, the finished Ariège search has `progress.google.state = "done"`, `progress.google.requests ≤ 30`, `progress.google.pinsVersion ≥ 1`, `googlePins.length > 0`, every pin `placeId` matching `/^[A-Za-z0-9_-]{10,72}$/`, no pin with a `name` key, ≥ 1 row with `googlePlaceId` and `onGoogle: true`, note `google_matched`; `notes[].text` contain none of the forbidden tokens nor *claimed* / *verified* / *tile* / *partial*. A `GET ?id=&after=<n>&pins=<pinsVersion>` slice carries no `googlePins`.
- **G4 — No Google content at rest.** After G3 and G6: `SELECT COUNT(*) FROM api_cache WHERE provider LIKE 'google%' OR payload LIKE '%displayName%' OR payload LIKE '%formattedAddress%' OR payload LIKE '%websiteUri%'` = 0; `SELECT COUNT(*) FROM prospects WHERE google_place_id IS NOT NULL AND google_place_id NOT GLOB '[A-Za-z0-9_-]*'` = 0.
- **G5 — Dismiss a Google pin.** `POST /api/admin/find/dismiss { searchId, key: "google:<placeId>" }` (a pin of the search) → `200` and the pin reads `hidden: true` on the next `GET`; `undo: true` reverses it; `key: "google:bad id"` → `400`; `key: "google:<well-formed id not in the search>"` → `404`.
- **G6 — Audit check.** `POST /api/admin/find/save` of a row with `googlePlaceId` → `GET /api/admin/prospects/{id}` shows `googlePlaceId`, `googleListing = "found"`, `googleMatch = "auto"`; after its audit, `audits.checks.google_listing.measured = true` with `points` in 0–10 and `details` keys ⊆ `{listing, operational, websiteOnListing, hours, reviews, photos, fetchedAt, checkedAt, reason, attributions}`; a saved row **with coordinates** and without a place id gets `googleMatch = "auto"` and `googleCheckedAt` set by its audit and, when nothing matched, `googleListing = "unverified"` with `details.reason = "no_match"` and `measured = false`; a prospect added by URL alone (no coordinates, no postcode) gets no `googleCheckedAt` and no `api_usage.google_search` increment from its audit; `POST …/google { action: "reject" }` → `not_found`, `googleMatch = "manual"`, `no-gbp` in the next audit's flags, and that audit leaves the columns untouched.
- **G7 — Caps and failures.** `GOOGLE_PLACES_MONTHLY_CAP=0` → `POST …/google { action: "check" }` → `429 google_monthly_cap` with `pool = "google_details_enterprise"` and no `api_usage` increment; `GOOGLE_SEARCH_MONTHLY_CAP=0` → a search finishes `done` with note `google_allowance` and `progress.google.state = "failed"`, `error = "allowance"`.
- **G8 — e2e, Google map and cards.** `/admin/find?search=<Ariège id>` renders no `.leaflet-container` and one `div[aria-label="Map of the businesses found"]` containing a `gmp-advanced-marker` (or `.dm-gcluster`), `window.__dmFindPins.total ≥ 250`; after a second search from the same page `window.__dmGmapLoads === 1`; clicking `__dmFindProject(<osm key>)` opens the business card; clicking a blue pin (`__dmFindProject("google:…")`) opens `[data-testid=google-only-card]` whose title is *On Google only* and which contains a **`gmp-place-details`** element (full, with a `gmp-place-content-config` child) and a link to `google.com/maps/place/?q=place_id:`; no text node of the card outside that element equals a Google business name (the e2e compares against the panel's rendered heading when the element's shadow root is open); the Add form inside the card has no name field; the *Only on Google* chip hides the blue pins.
- **G9 — e2e, prospect page.** `/admin/prospects/[id]` for a prospect with a place id: `[data-testid=google-block]` is a collapsed `<details>` placed after the audit section; before opening it, `main` contains no signal word (*website on the listing*, *opening hours*, *reviews*, *photos* in the Google sense) and no `img[src*="google-maps-logo"]`; opening it hides `[data-testid=mini-map]`, shows a status word from the §5.6 list without an attribution mark next to it, the signal words with the Google Maps logo image (`img[src*="google-maps-logo"]`), a *View on Google Maps* link, and no element in `main` contains *claimed*, *verified*, *GMB* or *Google My Business*; closing it brings the mini map back.
- **G10 — Build/test gates.** `npm test` green; `tsc` clean; `npm run build` once on staging; `git log main..feat/finder-google` shows only `finder-google: …` commits, no `.env*`; `package.json` diff = `@googlemaps/markerclusterer` + dev `@types/google.maps`; `public/brand/google-maps-logo.svg` and `-outlined.svg` present.

---

## 10. Sequencing and effort

Day 0 (backend, 2 h): types + schema + `.env.example` block + notes 17–25 + synthetic fixtures + `googleRequests.ts` masks and parsers — committed so the frontend has shapes and a sample. Day 0 (frontend, 1 h): the two logo files. Backend then: client with pools/reservations/caps/errors + tiles (incl. `ran`) + inside + match + the runner phase + children bboxes + progress/notes (1.75 d) → suggestion resolution + `resolveArea` hint (0.5 d) → `googleCheckRules` + `googleCheck`, scoring, report copy, the `/google` route, Add-by-URL place id + attach, name write-back, Google-only audits and allowance, Today cards + digest + purge step (1.5 d) → privacy notice / register / LIA edits, tests, README/spec pointers (0.75 d) = **4.5 d**. Frontend: loader + custom-element helper + CSP + `GoogleFindMap` (markers, clusterer, outline, projector, fallback, single-instance guard, attribution) (1.5 d) → `GoogleAreaInput` (ready gate, typed-text capture, pick flow) (0.75 d) → pins/chip/legend/progress + card sections + `GoogleOnlyCard` + Add-by-URL prefill (1 d) → `GoogleBlock` rewrite + `GoogleListingPanel` (two variants) + `AuditBlock` status words + `MiniMap` testid + Today lines (0.75 d) → wording/tests (0.25 d) = **4.25 d**. Integration: staging build, live checklist L0–L10 (needs the keys and Radu's L0 signature), e2e G1–G10, fixes = **1.25 d**. Total **10 engineer-days**; two people in parallel ≈ 6 calendar days, the last day gated on Radu's keys. Merge order: backend day 0 → frontend against the fixture → backend routes → integration on `feat/finder-google` → staging soak with Google off (G1) then on → prod (separate approval; L0 first; the prod restart needs Radu; prod `.env.local` gets the §6 block with `GOOGLE_PLACES_DISCOVERY` per D1).

## 11. Risks

- The UI Kit elements are young: the compact/full element split, the `gmp-select` payload (id only, per the reference) and the elements' CSS hooks may drift — L3 verifies, Appendix B is the autocomplete fallback, and the panels degrade to the *View on Google Maps* link.
- 2,000 Advanced Markers are DOM nodes; the clusterer keeps the visible count small, but a phone at zoom 9 over France with 2,000 pins may stutter — the clusterer's `radius` is the knob; Leaflet stays one env value away.
- One Dynamic Maps load per `FindWorkspace` mount — but a breakpoint change (desktop ↔ phone layout) remounts the map pane and costs one more load, and dev StrictMode is guarded; 10,000 free a month makes both harmless for one admin. Nobody should "fix" the pane placement into a leak by keying the map on the search.
- Discovery spends up to 30 Pro requests per search (5,000 free): ~130 searches a month before the reserve stops it; the note says so, listing checks continue, and the search still completes.
- Terms readings D1–D3 are Radu's; the defaults are the conservative ones and every Google feature is one env value to switch off. The prospect-facing notice and the register must be updated before Google is on in prod (L0).
- `NEXT_PUBLIC_*` values are baked at build time — a key rotation means a rebuild on staging and on prod.

---

## Appendix A — request/response shapes and SKU tiers (verified 12 Sep 2026)

- Base `https://places.googleapis.com/v1`; headers `X-Goog-Api-Key`, `X-Goog-FieldMask` (comma-separated, no spaces, `places.` prefix on search endpoints, none on Details); billed at the highest tier present in the mask.
- **Text Search** `POST /places:searchText` — `textQuery` (required), `includedType` (one Table A type), `strictTypeFiltering`, `locationRestriction.rectangle { low: SW, high: NE }` (rectangle only; categorical queries), `locationBias` (circle ≤ 50 km or rectangle; exclusive with restriction), `pageSize` 1–20, `pageToken` (max 60 results across pages; other params must not change between pages; `nextPageToken` must be in the mask), `rankPreference`, `languageCode`, `regionCode`. Tiers: IDs Only = `places.id, places.name, places.attributions, places.consumerAlert, places.movedPlace, places.movedPlaceId, nextPageToken`; Essentials-level fields (`addressComponents, addressDescriptor, adrFormatAddress, formattedAddress, location, plusCode, postalAddress, shortFormattedAddress, types, viewport`) bill **Pro** on search; Pro = `accessibilityOptions, businessStatus, containingPlaces, displayName, googleMapsLinks, googleMapsTypeLabel, googleMapsUri, iconBackgroundColor, iconMaskBaseUri, openingDate, photos, primaryType, primaryTypeDisplayName, pureServiceAreaBusiness, searchUri, subDestinations, timeZone, utcOffsetMinutes`; Enterprise = `currentOpeningHours, currentSecondaryOpeningHours, internationalPhoneNumber, nationalPhoneNumber, priceLevel, priceRange, rating, regularOpeningHours, regularSecondaryOpeningHours, transitStation, userRatingCount, websiteUri`; Enterprise + Atmosphere = reviews, summaries, serves*/dineIn/takeout…. `attributions[]` = `{ provider, providerUri }` — the provider names must be displayed with the data (ToS 3.2.4).
- **Nearby Search** `POST /places:searchNearby` — `locationRestriction.circle { center, radius ≤ 50,000 }` only, `includedTypes[]` (no text query — a null-type trade uses Text Search with `locationBias.circle` instead), `maxResultCount` 1–20, no pagination; same tiers.
- **Place Details** `GET /places/{id}?languageCode=&regionCode=` — IDs Only (free, unlimited) = `attributions, consumerAlert, id, movedPlace, movedPlaceId, name, photos`; Essentials (10,000 free) = `addressComponents, addressDescriptor, adrFormatAddress, formattedAddress, location, plusCode, postalAddress, shortFormattedAddress, types, viewport`; Pro (5,000) = `accessibilityOptions, businessStatus, containingPlaces, displayName, googleMapsLinks, googleMapsTypeLabel, googleMapsUri, iconBackgroundColor, iconMaskBaseUri, openingDate, primaryType, primaryTypeDisplayName, pureServiceAreaBusiness, subDestinations, timeZone, utcOffsetMinutes` (**not used by this contract**); Enterprise (1,000) = `currentOpeningHours, currentSecondaryOpeningHours, internationalPhoneNumber, nationalPhoneNumber, priceLevel, priceRange, rating, regularOpeningHours, regularSecondaryOpeningHours, transitStation, userRatingCount, websiteUri`. `businessStatus ∈ OPERATIONAL | CLOSED_TEMPORARILY | CLOSED_PERMANENTLY | FUTURE_OPENING | BUSINESS_STATUS_UNSPECIFIED`. No claimed/verified/owner field exists.
- **Place types**: Table A includes `restaurant, bar, hotel, bed_and_breakfast, campground, bakery, butcher_shop, hair_salon, beauty_salon, car_repair, plumber, electrician, painter, roofing_contractor, real_estate_agency, dentist, gym`; `carpenter` and `optician` are not filterable (text query only). Table B (`(regions)`, `(cities)`, `locality`, `administrative_area_level_1/2`, `country`, `postal_code`) only in Autocomplete `includedPrimaryTypes`.
- **Places UI Kit** (Maps JS `places` library): `gmp-basic-place-autocomplete` (properties `includedPrimaryTypes, includedRegionCodes, locationBias, locationRestriction, origin, requestedLanguage, requestedRegion, placeholder, …`; event `gmp-select` = `PlaceSelectEvent` whose `place` **contains only the place id**; the element **clears its input field when a prediction is selected**). Place details: `gmp-place-details` (the full element; `orientation`, read-only `place` "containing the ID, location, and viewport of the currently rendered place"; events `gmp-load`, `gmp-error`) and `gmp-place-details-compact`, both taking `gmp-place-details-place-request place="<id>"` and either `gmp-place-standard-content` / `gmp-place-all-content` or an explicit `gmp-place-content-config` with child elements. **Child-element support**: the compact element supports only `gmp-place-media`, `gmp-place-address`, `gmp-place-rating`, `gmp-place-type`, `gmp-place-price`, `gmp-place-accessible-entrance-icon`, `gmp-place-open-now-status` and `gmp-place-attribution`; `gmp-place-website`, `gmp-place-phone-number`, `gmp-place-opening-hours` (and reviews, summaries, feature list, type-specific highlights) are **supported only in `gmp-place-details`** — hence the full element wherever the website must be visible. EEA SST B.15.3: the map and permitted-use restrictions "do not apply to the Places UI Kit". Billing on Maps JavaScript: both details elements and the autocomplete element are the *Places UI Kit Query* / *Autocomplete Per Session* SKUs (10,000 free each, then $1/1,000 and $10/1,000); the *Places UI Kit Pro* SKU is triggered only by the iOS Pro components, not by the web elements.
- **Maps JavaScript**: one Dynamic Maps load per `new google.maps.Map()` (10,000 free, $7/1,000); pans and zooms are free; Advanced Markers need a `mapId` (`DEMO_MAP_ID` for tests); `@googlemaps/markerclusterer@2.6.2` accepts `AdvancedMarkerElement`; `@types/google.maps@3.66` (element classes, no JSX declarations).
- **Attribution policy**: "Attribution should take the form of the Google Maps logo whenever possible. In cases where space is limited, the text Google Maps is acceptable." — logo assets (standard and outlined) are provided on the attribution page; the text form is used here only for the list-header count.
- **Pricing model** since 1 Mar 2025: per-SKU free monthly events (Essentials 10,000 / Pro 5,000 / Enterprise 1,000; some Essentials unlimited), reset on the 1st at midnight **Pacific** (hence the Pacific day keys of §4.2).

## Appendix B — fallback for D5: server-proxied Autocomplete (New) with session tokens

Only if L3 shows the UI Kit element unusable (styling, keyboard, a broken `gmp-select`, or no observable `input` events), and only with Radu's acceptance that predictions listed above the map are Google content next to a map (EEA SST B.15.1, permitted use (1)):

- `POST /api/admin/find/suggest { input: string (2–120 chars), sessionToken: uuid v4 }` (admin, CSRF, `rateLimit("google-suggest:" + ip, 60, 1 min)`, `maxDuration 10`) → `POST /places:autocomplete { input, sessionToken, includedPrimaryTypes: ["(regions)"], languageCode: "en" }` (no mask) → `{ ok, suggestions: [{ placeId, main, secondary, types }] }` (≤ 5, ≤ 120 chars each; transient). Counted as `google_autocomplete` (10,000 free) with its own cap `GOOGLE_AUTOCOMPLETE_MONTHLY_CAP=9000`.
- The client keeps one token per typing session (`crypto.randomUUID()`), debounces 250 ms, renders a `role="listbox"` under the Area field (arrow keys, Enter, Esc, `aria-activedescendant`), and on pick posts `/api/admin/find` with `suggestion: { placeId, sessionToken }` and `area` = the typed text (a plain input — always readable here); the server closes the session with the Details **Essentials** call of §4.4 carrying `?sessionToken=` (autocomplete requests beyond the first 12 in the session are then free); the token is discarded after the close. An abandoned session bills per request — within the free tier at our volume. `GoogleAttribution` (logo) sits at the bottom of the list.

## Appendix C — decisions log (integrator only)

- 12 Sep 2026 — v1 published on `feat/finder-google` (from `main` `b778ee8`). Defaults: D1 discovery off, D2 derived signals stored, D3 website not copied, D4 Google map when the browser key + Map ID are set, D5 UI Kit autocomplete. Awaiting Radu's billing and keys; the live checklist is the last gate.
- 12 Sep 2026 — v2 after review round 1. Terms/cost: full `gmp-place-details` (website visible) for the Google-only card and the prospect page, compact only on the business card; `gmp-select` carries the id only and clears the box → `suggestion: { placeId }`, typed text kept client-side, `400 suggestion_unresolved` when there is nothing to fall back on; derived words only inside the collapsed Google section (D2 applied everywhere; `AuditBlock` and the report show status words); an automatic match miss leaves `google_listing = unverified` (only a manual *Not this listing* yields `not_found`/`no-gbp`), and the match is skipped without coordinates or postcode; prospect-facing notice, Art. 30 register and LIA edits added as backend work and gate L0; the Google Maps logo committed day 0 and always rendered (text only in the list-header count), no existence flag; `attributions` kept with the signals and candidates and rendered as *Data: …*; per-day Cloud Console quotas as the browser-key hard stop; D2 extended to the report/drafts with status words only and the pass copy changed to *en place et complète*; the Google-only name is taken from the site, never typed from the panel; no attribution on status words; Pacific-day counters and reservation before the socket; Details Pro dropped (candidates from Text Search Pro); D1 rationale cites 3.3.2(a)(ii) and (d); null-type trades on circle areas use Text Search with a bias circle; one Map instance per workspace mount with a load counter; discovery stops 500 short of the search cap. Engineering fit: *parts of {area}* wording (no *tile*/*partial* in any text); `googleCheckRules.ts` (pure) + `googleCheck.ts` (I/O); `continue` unchanged, *Run again* re-asks Google; `fresh` job flag dropped; Today card contract (`google`, `google-key`); dismiss accepts Google pins, `placeIdOk {10,72}`; `GooglePin.saved` and `placeAttached` on Add by URL; UI Kit elements created imperatively (no JSX declarations, SSR-safe); Google-only audits at priority 8 with `GOOGLE_CHECK_DAILY_CAP` and `auditNext` covering them; capped searches use unit bboxes (children now carry one); Area box `ready` gate and typed-text capture; page block order allowed, `mini-map` testid; Prospect Google fields optional; Retry-After only for the runner; notes tokens 13 with whole-word care; `googlePins` only when the phase ran and only on full reads or a new `pinsVersion`; StrictMode/breakpoint map-load note.
- 12 Sep 2026 — merged into `main` (`4b7d0ed`, `--no-ff`; conflicts with the "bigger type" commit resolved by taking the branch files and applying its one-step px bump to the Google UI). Tests 327/327, `tsc` clean. Keys pending: `docs/finder-google-live-checklist.md` is the last gate (Part A console steps for Radu, Part C acceptance L0–L10 / G1–G10).
